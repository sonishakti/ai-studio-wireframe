# User test #13 — 2026-07-14 · Deploy-copy truthfulness (commit `c9651a6`, walked on `e11513d` live)

> Personas walked the live build AFTER the run-mode TABS shipped (`e11513d`), so this
> round also covers the tabs in situ.

## Metrics

Task success **90%** · TTFA-interaction 1 · TTFA-live **6** · 0×S1 · 3×S2 · 11×S3 ·
sentiment **1.0** · SUS-lite 82.7. D1 went cold-landing → live batch in 6 clicks.

## Verdict

The copy-integrity commit "survives all three personas on its own terms" — D2 verified
all five commit strings in the live bundles; every persona cited the fixed surfaces as
trust-builders: "the button and the sentence tell the same story" · "disabled-with-reason
instead of vanishing like on Retell" · "one story, matching verbs." Zero S1/S2 on the
changed copy. The no-op guard passed its second deliberate trust-probe ("didn't fake a
success. Respect.").

## Residuals (next slice, pinned)

1. **P0** — stay-path deploy on a LIVE agent doesn't advance the dirty baseline: after a
   code repoint deploys, edits can still read as pending (state one layer under the copy).
2. **P0** — caller-ID disabled reason branches wrong for DRAFT-agent-assigned numbers:
   "assigned to Sales Qualifier — a line can't answer and dial at once" when Sales
   Qualifier is a draft that answers nothing. Reason must fit the assignee's status
   (and the repoint case: "frees up when the batch starts" — this line stops answering).
3. **P1 recurring** — the simulated Talk transcript still ignores the configured
   greeting/prompt (flagged every round since #6; it's the only pre-deploy verification).
4. S3s: "Redeploy" as resting label on an untouched live agent; Live-badge truth
   hover-only; no dial-my-own-phone proof path.
