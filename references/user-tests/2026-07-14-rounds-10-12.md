# User tests #10–#12 — 2026-07-14 · Escape hatches · two-CTA rail · Code third state

> Three rounds on commits `608eae4` (no one-way doors) → `5203a83` (two bare CTAs,
> Talk first) → `860dbaf` (Code/SDK "Deployed" state + honesty fixes). Protocol:
> `references/user-testing-protocol.md`.

## Metrics (trend)

| Metric | #12 | #11 | #10 | #9 | Read |
|---|---|---|---|---|---|
| Task success | 86.7% | 86.7% | 86.7% | 86.7% | steady |
| Friction | 0×S1 · **3×S2** | 0×S1 · 4×S2 | 0×S1 · 5×S2 | 0×S1 · 6×S2 | S2s halved across the day |
| Sentiment | +0.91 | +0.89 | +0.87 | +1.11 | high plateau |
| SUS-lite | **85.3** | 84.6 | 84.2 | 84.0 | third record in one day |

## Verdicts

- **#10 (escape hatches):** validated — D1: "Every branch has a door back now… knowing
  nothing is a trap is why I'm still clicking at 11pm." Its S2 (caller-ID omits the
  agent's own number silently) was fixed in `860dbaf`.
- **#11 (two bare CTAs, Talk first):** validated — the order "mirrors exactly how I
  wanted to work: believe (talk), then scale (deploy)." Its S3s (Saved/Unsaved
  vocabulary collision; fake no-op redeploy toast) were fixed in `860dbaf`.
- **#12 (Code third state):** passes across all three personas — D3: "**the first wizard
  state machine I've trusted**"; D1 on the no-op guard: "It refused to fake a deploy.
  That buys real trust for every toast after this." Caller-ID disabled-with-reason
  converted "where is my number?" into stated policy.

## Open (queued as next slice — all copy/filter-level)

1. Branch-aware deploy success toast: code path should say "deployed — goes live when
   your app connects", not "is live"; and "snippets below" sits above the toast.
2. Voice-browser per-row Play toast needs the same simulated-audio disclosure.
3. `pn_09` filter fallthrough in the caller-ID candidates (agent-assigned numbers).
4. Step-publish promise line contradicts a repointed CTA ("Launch batch calls" over
   "Redeploying applies your edits…").
5. Recurring themes: real-audio preview, CSV column remap UI, number provenance,
   batch pause promise, repoint-aware caller-ID ("this line frees up when the batch starts").

## Durable learning

**Honesty compounds and personas test it deliberately.** D1 clicked no-change Redeploy
*as a trust probe* before believing the Live badge — the "Already live — no changes"
refusal converted one guard into blanket toast credibility. Every truth mechanism
shipped this cycle (embed truth, deploy truth, third state, no-op guard) was
independently exercised and cited as the reason to keep going.
