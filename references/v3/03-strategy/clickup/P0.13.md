🏷 **V3 · launch Fri 16 Oct 2026** · P0 Agent config · P0.13 · design ETA **Tue 29 Sep** · lock ETA **Thu 1 Oct** · sheet status WIP

**Status is the queue.** Open = planned · in progress = the design agent takes it at the next run (08:00, 11:59, 16:00, 20:00) · in review = delivered, pending Shakti's review · completed = approved and locked (Figma section frozen, commit recorded) · on hold = parked. To ask for changes: move it back to in progress and comment `change: …`.

## Job to be done (Sam, a developer)
**Job:** Try it without counting · job map stage **Prepare** (Sam sets things up so the job can run)
**Umbrella job (P0):** Sam wants to create a new agent that people outside the team can talk to.
**Situation:** Sam tries the agent as often as needed without those tries counting as production sessions or in the agent's numbers.
**Sam wants to:** test freely
**So that:** tests never inflate the agent's production numbers

### P0.13.a Happy path (this is what Sam does)
1. Studio stores every session id it starts, where clearing the browser cannot lose it, and tags client_reference studio_test:.
2. Test sessions carry a Test tag and stay out of agent numbers and production counts (P1.2, P2.1).
3. Enabler (team): builder and test events port from event-spec 1.0.0 into ng-console, and the sanitiser allowlist grows.
4. Enabler (team): renames ship once, in the next schema bump, with no aliases.
5. Enabler (team): data-quality checks run green for 7 days before any P0 KPI is reported.

### Rainy paths (each needs its own state in the prototype and a frame in Figma)
*   **P0.13.b** Preview and local traffic count as external (classifyNonProductionAsTest): Fix this before any preview deploy.
*   **P0.13.c** The sanitiser drops agentId, turnCount, configuredByUser, deploymentType, every new key and all arrays: Extend the 50-key allowlist and send fields pipe-joined.
*   **P0.13.d** There is no API gateway emitter (G1): Console stages fall back to their operation_succeeded twins. API and ephemeral journeys stay unmeasured, and reports say so.
*   **P0.13.e** Session and agent ids do not match (SessionListItem.agent_id is 32 lowercase characters; Agent.id has the agent_ prefix): Confirm the join key before stage 10 is reported.
*   **P0.13.f** A stored test id is lost: client_reference is never returned, so the stored id is the only proof of a test. Store ids where clearing the browser cannot lose them, or the test counts as production.
*   **P0.13.g** Sam blocks analytics or declines consent: Nothing is tracked without consent. Server events still count stages 3, 5 and 9, and reports show the measured share.

## Goal (KPI)
**In one line:** 98 in 100 agents and tests are counted, among consented users.
*   Metric: Event completeness
*   Target: At least 98 %, among consented users
*   How we measure: agent_test_started whose session id is in the Studio registry, share of agent_test_started; agent_created {source console} with a prior builder_opened, share. Both over consented users only.
*   Counter-metric: Each data-quality check must read 0: untagged Studio tests; ephemeral sessions in agent aggregates; suspensions with no 80 % warning; an error badge showing 0 while the stream is down
*   Assumption we are testing: Ported client events plus Studio-stored test ids can compute TTFA, FPR and Test before Go live from day one. Method: data-quality checks green for 7 consecutive days before any KPI is reported.

## Telemetry to configure before this ships
| Event | Fired by | Status |
| --- | --- | --- |
| `builder_opened (port 1.0.0)` | client (PostHog) | new client event (PostHog) |
| `agent_test_started (port 1.0.0)` | client (PostHog) | new client event (PostHog) |
| `agent_audio_heard (port 1.0.0 + new props surface, agentVersion; agentVersion needs an allowlist key)` | client (PostHog) | new client event (PostHog) |
| `page_viewed` | client (PostHog) | in 2.1.0 |
| `operation_succeeded` | client (PostHog) | in 2.1.0 |
| `agent_created` | server | server, API gateway emitter |
| `agent_updated` | server | server, API gateway emitter |
| `channel_connected` | server | server, API gateway emitter |
| `session_ended (G2, no owner)` | server | server, API gateway emitter |

**Funnel stages this moves:** 3 to 12, every builder and deployment stage
**Server gaps that block the read:** G2 (see the sheet, Server gaps)
**Where it is read:** PostHog (client), API gateway emitter (server), cohort read 14 d after the week. KPI owner: Agent config umbrella.

## Research
| Competitor | Status | Note |
| --- | --- | --- |
| Vapi | Partial | Product and Docs. v3/02-research/monitoring/shots/vapi-04-call-logs-list.png shows a Type column and a Call Types filter; competitors/product/vapi/vapi-12-logs-ended-reason.png shows the same filter and a Web type in the product. No shot of tests left out of counts or billing. |
| Retell | Partial | Docs. v3/02-research/monitoring/shots/retell-01-call-history-list.png shows a channel type column separating web_call from phone_call. No test flag. |
| ElevenLabs | Partial | Product. competitors/product/elevenlabs/elevenlabs-13-conversation-history-list.png shows the conversation list. Test marking was not verified. |
| LiveKit | Partial | Product. competitors/product/livekit/livekit-15-agent-console-configuration-sheet.png: the Console test targets a deployment, 'Leave as production' by default, so tests can run on a non-production deployment. livekit-12-sessions-live.png and livekit-sessions.png show session lists with no test flag. |

**Still owed:** Evidence so far: Retell separates web_call in its channel type, Vapi has a Call Types filter, and the LiveKit Console can target a non-production deployment. Still missing at every vendor: tests left out of agent counts and billing. Check ElevenLabs test marking. Desk notes: v3/02-research/competitor-monitoring.md and observability-patterns.md.

## Deliverables (filled by the agent, locked by Shakti)
*   Prototype (opens at the start of Sam's journey): after the run
*   Figma flow, page "v3 · P0 Agent config", section "P0.13 · Try it without counting": after the run
*   Lock: commit on design/v3 once approved
*   Sheet row: https://claude.ai/artifact/6YZCRsJvpoBthj4fnm9XAb
*   Research folder: references/v3/features/P0.13/

## Agent brief (read before designing)
**Scope (sub-tasks):**
- [ ] Port builder and test events to ng-console
- [ ] Allowlist PR: 15 new keys, fields pipe-joined
- [ ] Rename map, once, no aliases, next schema bump
- [ ] Retire agent_publish and deployment status operations
- [ ] Server to client prop name map
- [ ] Durable store for Studio test ids
- [ ] Dashboard: TTFA, TTFDA, Test before Go live, FPR, with measured share
- [ ] Data-quality checks, 7 days green first
**API:** Partial. client_reference is writable on SessionCreate but never returned. Missing: purpose field, webhooks and events in the spec; the gateway emitter is unbuilt, as are the emitters for notif 112, 201 and 202.
**Register rows:** 56 (new), 36. Enabler (team), not a job step: the team sees where Sam stalls while creating an agent.
**Journeys:** B · Build loop · INT · Integration · IN · Inbound in Studio · BA · Batch run in Studio · CO · Code in Studio · API · API-only, saved agent · EP · Ephemeral only · ST · Stalled at Go live · SC · Stalled after own session · FX · Fix loop
**Design days budgeted:** 0.25
**Rules:** existing Console design system only (docs/design/DESIGN.md on design/v3); reuse, do not redesign; empty first, quiet chrome; sentence case, no arrows or em dashes in prose; locked words from the sheet vocabulary; the persona is Sam and appears only in job text; never change a locked feature unless in scope, then say so in the review post.
**Output of one run:** 1 prototype on design/v3 reachable by URL for the happy path and every rainy state · 2 one screenshot per step of the happy path and per rainy state · 3 the Figma flow: frames in step order, each captioned "Sam does …" with the rationale under it, research shots marked in red · 4 this task moved to in review with a comment (prototype, Figma, commit, rationale) · 5 the sheet row updated · 6 the Slack post "JTBD n of 42 · <id> <title> is done and pending your review", prototype link first, then Figma, then 3 to 5 rationale lines, then open questions.