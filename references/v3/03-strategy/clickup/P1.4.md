🏷 **V3 · launch Fri 16 Oct 2026** · P1 Monitoring · Agents · design ETA **Wed 30 Sep** · lock ETA **Fri 2 Oct**

## Job to be done (Sam, a developer)
**Job:** Find what is failing and why · job map: Locate
**Umbrella job (P1):** Sam wants to know how each agent is doing with people outside the team, and fix what fails before customers notice.
**Situation:** Sam finds out what is failing and why, so each cause is fixed once, not session by session.
**Story:** Sam sees '2 error groups' on the agent and wants the cause, so Sam can fix it once instead of chasing six sessions.

### P1.4.a Happy path: what Sam does
1. Logs and diagnostics (agent tab /agents/:id/logs, project view /logs) lists groups '<module> · <event type>' with severity, sessions, first and last seen, a sparkline and tags New, Quiet, Returned, New since change; every cell is a link.
2. Sam opens a group; the panel shows count over time with the last-change marker and 'Since your change: N occurrences'.
3. Sam steps through the affected sessions one by one; a sample line shows the failing log line.
4. Doors lead to the setting, secret set, integration, run or number (P1.6), plus 'View all N sessions'.
5. A separate tests line links to test sessions, kept apart from production.
6. The project view adds 'No agent' and 'Agent not linked' buckets and groups from deleted agents.

### Rainy paths: when it goes wrong
*   **P1.4.b** No errors in range: 'No errors since <date>'.
*   **P1.4.c** Error stream unavailable: 'Errors unavailable', never 'No errors'.
*   **P1.4.d** Failure with no session (session never created, billing, number): 'No session' rows in the project view with a door to the number or billing.
*   **P1.4.e** A run failed: Campaign.failure shows as one group, linked to the run.
*   **P1.4.f** No stable error code: Key <module_name>:<event_type>, marked approximate; warnings hidden while none can be derived.
*   **P1.4.g** Errors from a deleted agent: The group stays in the project view, tagged 'Agent deleted'; doors are disabled with the reason.
*   **P1.4.h** A group key changes after a code update: The old key redirects to its successor; links keep working.
*   **P1.4.i** Session content not kept (zero retention or expired): The count stays; the sample line reads 'Zero retention' or 'Expired', never empty.
*   **P1.4.j** A fixed error comes back: Tag Returned, listed first, with 'Since your change: N occurrences'.

## Goal (KPI)
**Median path from an error to its cause is at most 4 steps and under a minute.**
*   Metric: Error to root cause
*   Target: Median ≤ 4 steps and ≤ 60 s (p75 ≤ 6 steps and ≤ 120 s). Details: acknowledgement median ≤ 24 h.
*   How we measure: Per visit (visitId, 30 min inactivity): first error_badge_clicked or error_group_opened to first log_line_opened {severity: error} in that group's session; steps = monitoring_pivot count, seconds = delta, median over visits that reach it. Acknowledgement (PRD): first session_ended {status failed} or new error group to error_group_opened or logs_opened for that agent, median. Blocked on the RTM stream (G11) and session_ended (G2); interim: v2 debugging has_error.
*   Counter-metric: Log line left in under 5 s ≤ 30 %; groups unopened at 7 d ≤ 10 %
*   Assumption: Grouping by cause with a one-step path to the failing line beats a flat list. Method: moderated 5-owner baseline on the current Console; kill if the median exceeds 8 steps or 3 min.

## Telemetry
| Event | Fired by | Status |
| --- | --- | --- |
| `logs_opened {scope: agent|project}` | client (PostHog) | new client |
| `error_group_opened {groupKey, module, surface, tags}` | client (PostHog) | new client |
| `monitoring_pivot {fromLevel: errgroup}` | client (PostHog) | new client |
| `session_opened {surface: error_group}` | client (PostHog) | new client |
| `log_line_opened {severity, module, surface: error_group}` | client (PostHog) | new client |
| `empty_state_viewed {surface}` | client (PostHog) | in 2.1.0 |
| `session_ended (G2, no owner)` | server | server |
| `error_group_quiet (derived, needs G11)` | server | server |
| `session_create_failed (G1, G16, no owner)` | server | server |

Funnel stages this moves: 12 session_reviewed (surface logs)
Server gaps that block the read: G1, G2, G11, G12, G16

## Research
| Competitor | Status | Note |
| --- | --- | --- |
| Datadog | Done | Docs. v3/02-research/monitoring/shots/datadog-06-error-tracking-issue-details.png and datadog-05-log-side-panel-context.png: lifecycle strip, New and Regression, samples with linked logs. |
| LiveKit | Done | Docs and Product. v3/02-research/monitoring/shots/livekit-03-transcript-traces-logs-tabs.png (session logs) and competitors/product/livekit/livekit-13-agents-observability-dashboard.png ('Dispatch errors' with no drill, avoid). |
| Vapi | Done | Product and Docs. competitors/product/vapi/vapi-12-logs-ended-reason.png and sip-call-detail-ended-reason.png, v3/02-research/monitoring/shots/vapi-01-debugging-overview.png; Monitors and Issues from desk only (competitor-monitoring.md). |
| Retell | Done | Docs and Product. v3/02-research/monitoring/shots/retell-03-alerting-rules.png and competitors/product/retell/retell-13-alerting-*.png: rule alerts, no grouping by cause. |
| ElevenLabs | Done | Docs. v3/02-research/monitoring/shots/elevenlabs-03-tools-tab-error-rate-latency.png: tool error rate with no drill (avoid). |
| Sentry | Done | Docs. v3/02-research/monitoring/shots/sentry-01-issue-details-breakdown.png and sentry-03-trace-related-issue.png: 'View More Events', sibling issues. |
| Twilio | Done | Docs. v3/02-research/monitoring/shots/twilio-04-alarms-config.png: alarms with no link to the session that tripped them (avoid). |

Still owed: Owed: Vapi Monitors and Issues as shots (desk only today); Retell alert history with a fired alert. Baseline task: 5 owners on the current Console, timed from error to cause.

## Deliverables
*   Prototype: not yet
*   Figma flow: not yet (page v3 · P1 Monitoring · Agents, section P1.4 · Find what is failing and why)
*   Commit: not yet
*   Sheet row: https://claude.ai/artifact/6YZCRsJvpoBthj4fnm9XAb#P1.4

---

###### Footnote for the design agent
Scope: Group row with tags and linked cells; Group key and approximate marker; key versioning and redirects; Project view with No agent and Agent not linked buckets; Panel: count over time, change marker, since-change count, affected list with in=group; States: no errors, unavailable, quiet, returned, loading.
API: Missing in v3. No error stream or stable codes (G11); no error filter (G12). Partial through v2 events and debugging tasks has_error (decision 15). Campaign.failure works. Sessionless errors need session_create_failed (G1, G16). Register: 27, 28, 29; 57 (new) Journeys: IN · Inbound in Studio · BA · Batch run in Studio · CO · Code in Studio · FX · Fix loop · FX-R · Fix with nothing to read · INT-R · Integration saved, not working. Days: 0.5.
Rule 0, data first. This flow shows: production sessions, error groups and log lines, phone numbers, batch runs and contact lists, secrets and provider keys, integrations (MCP servers, tools), billing, free minutes, a card, several agents. New account: A new account has no production sessions, no errors and no runs: every monitoring view opens empty. The empty state is a rainy scenario, never the research. Outside our accounts: LiveKit and Retell signed-in accounts hold sessions and call history; Datadog, Sentry and Honeycomb docs for error groups and traces (shots in v3/02-research/monitoring); Twilio docs for call logs; Bland for batch lifecycle. Create in our account: In our account: 2 agents, 10 test sessions of which 3 forced failures (invalid BYOK key, unreachable number, idle timeout), 1 batch run with 5 contacts, 1 number pointed at an agent, then wait for the sessions to land in history. Note the app id and the session ids for the screenshots.
Files: references/v3/features/P1.4/ (jtbd, directions, build spec, flow/ screenshots, shots/ research).
Status: Open = planned · in progress = taken at the next run (08:00, 11:59, 16:00, 20:00) · in review = pending Shakti · completed = locked (Figma frozen, commit) · on hold = parked. Changes: back to in progress + comment `change: …`.
Rules: existing Console design system only (docs/design/DESIGN.md, design/v3); reuse, do not redesign; empty first, quiet chrome; sentence case, no arrows or em dashes; locked words; Sam only in job text; never change a locked feature unless in scope, then say so. One run delivers: prototype for the happy path and every rainy state, one screenshot per step, the Figma flow with "Sam does …" captions and rationale, this task in review with a comment, the sheet row, one Slack post.