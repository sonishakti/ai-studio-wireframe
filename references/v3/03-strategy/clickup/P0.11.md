🏷 **V3 · launch Fri 16 Oct 2026** · P0 Agent config · P0.11 · design ETA **Tue 29 Sep** · lock ETA **Thu 1 Oct** · sheet status WIP

**Status is the queue.** Open = planned · in progress = the design agent takes it at the next run (08:00, 11:59, 16:00, 20:00) · in review = delivered, pending Shakti's review · completed = approved and locked (Figma section frozen, commit recorded) · on hold = parked. To ask for changes: move it back to in progress and comment `change: …`.

## Job to be done (Sam, a developer)
**Job:** Connect the team's software · job map stage **Execute** (Sam does the core job)
**Umbrella job (P0):** Sam wants to create a new agent that people outside the team can talk to.
**Situation:** If the team's own software will start sessions, Sam connects it to the agent and knows the first session worked.
**Sam wants to:** the team's app to talk to the agent
**So that:** its users get answers inside the app

### P0.11.a Happy path (this is what Sam does)
1. In Deployment, on the code tab, Sam picks rtc or telephony, and curl or one SDK.
2. The POST /sessions snippet arrives filled with agent_id, transport, lifecycle and data_policy. The App ID, the RESTful API key link and a token note sit beside it.
3. Sam copies the snippet and runs it.
4. The tab confirms the first session for this agent.

### Rainy paths (each needs its own state in the prototype and a frame in Figma)
*   **P0.11.b** The first session fails (401, 403, 422): The tab shows the error code, the field at fault and one fix link.
*   **P0.11.c** The session ends at the idle timeout with nobody there: Sam enters the RTC channel name, and Studio checks it against GET /sessions?channel=.
*   **P0.11.d** No session 24 h after the copy: A status strip (P1.8) shows the snippet again.
*   **P0.11.e** Sam's own dev sessions look like production: Studio never writes a tag into the customer's snippet. Developer sessions count as production, marked provisional until a purpose field ships.
*   **P0.11.f** Sam does not want a saved agent: An ephemeral snippet sits on a secondary tab, marked account level only.
*   **P0.11.g** Sam is about to put the RESTful API key in browser code: The snippet marks the key as server-side only, and the token note shows the client path.
*   **P0.11.h** The failed request never reaches Studio (no gateway emitter, G1): The tab says no session has been seen yet and lists the three common errors with their fixes.

## Goal (KPI)
**In one line:** Median time from copying the snippet to the first session under 30 minutes.
*   Metric: Snippet to first session
*   Target: Median 30 min wall clock or less
*   How we measure: code_snippet_copied to first_session_detected (client, from polling GET /sessions for the agent), median. Replace the end with channel_connected {code} once the gateway emitter ships (G1).
*   Counter-metric: Snippet copies with no first_session_detected in 7 d: 40 % or fewer
*   Assumption we are testing: A filled-in snippet gets a first session within 30 min of the copy. Method: cohort read. Kill if the median is over 2 h (then add a quickstart repo link).

## Telemetry to configure before this ships
| Event | Fired by | Status |
| --- | --- | --- |
| `go_live_clicked` | client (PostHog) | new client event (PostHog) |
| `code_snippet_copied {transport, language}` | client (PostHog) | new client event (PostHog) |
| `first_session_detected` | client (PostHog) | new client event (PostHog) |
| `go_live_error_shown {errorCode}` | client (PostHog) | new client event (PostHog) |
| `rtc_channel_checked {found}` | client (PostHog) | new client event (PostHog) |
| `channel_connected {code} (G1, no owner)` | server | server, API gateway emitter |
| `session_create_failed (G1, G16, no owner)` | server | server, API gateway emitter |
| `agent_answered_production (derived, needs G2)` | server | server, API gateway emitter |

**Funnel stages this moves:** 9 channel_connected {code} (needs G1) · 10 agent_answered_production (derived, needs G2)
**Server gaps that block the read:** G1, G2, G16 (see the sheet, Server gaps)
**Where it is read:** PostHog (client), API gateway emitter (server), cohort read 14 d after the week. KPI owner: Agent config umbrella.

## Research
| Competitor | Status | Note |
| --- | --- | --- |
| Vapi | Done | Docs. competitors/public-docs/vapi-09-quickstart-assistant.png (create programmatically, a CLI snippet, a web calling section) and competitors/marked/vapi-09-dashboard-quickstart.png (Dashboard, TypeScript, Python and cURL tabs). No in-product snippet or first-session state. |
| Retell | Partial | Docs. competitors/public-docs/retell-09-quickstart.png is the phone agent quickstart and holds no code snippet. The 'Make a web call' page is listed in the docs nav in v3/02-research/monitoring/shots/retell-02-call-detail-panel.png but was not shot. |
| ElevenLabs | Partial | Product and Docs. competitors/product/elevenlabs/elevenlabs-18-widget-channel-embed.png shows embed code, not a session-start snippet. Also public-docs/elevenlabs-12-conversation-events.png. |
| LiveKit | Partial | Product and Docs. The Code tab shows, not opened, in competitors/product/livekit/livekit-19-agent-builder-conversation.png. Also public-docs/livekit-09-starter-apps.png. |

**Still owed:** No vendor shot shows first-session detection or dev traffic kept apart from production. Open the LiveKit builder Code tab, the Vapi web call snippet in the product, and Retell's 'Make a web call' docs.

## Deliverables (filled by the agent, locked by Shakti)
*   Prototype (opens at the start of Sam's journey): after the run
*   Figma flow, page "v3 · P0 Agent config", section "P0.11 · Connect the team's software": after the run
*   Lock: commit on design/v3 once approved
*   Sheet row: https://claude.ai/artifact/6YZCRsJvpoBthj4fnm9XAb
*   Research folder: references/v3/features/P0.11/

## Agent brief (read before designing)
**Scope (sub-tasks):**
- [ ] Snippet tabs: rtc, telephony; curl plus one SDK
- [ ] App ID, RESTful API key link and token note inline
- [ ] Server-side-only mark on the key
- [ ] First-session detected, not-seen-yet and error states
- [ ] RTC channel check field
- [ ] Ephemeral snippet, marked account level
**API:** Ready: POST /sessions {agent_id, transport, lifecycle, data_policy} and the GET /sessions?channel= filter. Missing: purpose field. client_reference is not returned, and list rows lack agent_session_id.
**Register rows:** 6, 36
**Journeys:** CO · Code in Studio · CO-R · Code first session fails · API · API-only, saved agent · EP · Ephemeral only · ST · Stalled at Go live
**Design days budgeted:** 0.125
**Rules:** existing Console design system only (docs/design/DESIGN.md on design/v3); reuse, do not redesign; empty first, quiet chrome; sentence case, no arrows or em dashes in prose; locked words from the sheet vocabulary; the persona is Sam and appears only in job text; never change a locked feature unless in scope, then say so in the review post.
**Output of one run:** 1 prototype on design/v3 reachable by URL for the happy path and every rainy state · 2 one screenshot per step of the happy path and per rainy state · 3 the Figma flow: frames in step order, each captioned "Sam does …" with the rationale under it, research shots marked in red · 4 this task moved to in review with a comment (prototype, Figma, commit, rationale) · 5 the sheet row updated · 6 the Slack post "JTBD n of 42 · <id> <title> is done and pending your review", prototype link first, then Figma, then 3 to 5 rationale lines, then open questions.