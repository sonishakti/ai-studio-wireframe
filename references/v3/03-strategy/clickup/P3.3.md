🏷 **V3 · launch Fri 16 Oct 2026** · P3 v3 mapping · P3.3 · design ETA **Wed 7 Oct** · lock ETA **Fri 9 Oct** · sheet status Planned

**Status is the queue.** Open = planned · in progress = the design agent takes it at the next run (08:00, 11:59, 16:00, 20:00) · in review = delivered, pending Shakti's review · completed = approved and locked (Figma section frozen, commit recorded) · on hold = parked. To ask for changes: move it back to in progress and comment `change: …`.

## Job to be done (Sam, a developer)
**Job:** Decide how sessions on numbers end · job map stage **Define** (Sam decides what the job needs before doing it)
**Umbrella job (P3):** Sam wants to look after everything the team's agents share, by hand or from code, and get the same result either way.
**Situation:** Across all the team's numbers, Sam makes sure sessions end cleanly and reach a person when the agent should stop.
**Sam wants to:** the support number to close silent sessions and hand upset callers to a person
**So that:** no session hangs open or dead-ends

### P3.3.a Happy path (this is what Sam does)
1. Each number row shows a one-line policy summary, for example 'Ends after 10 min or 30 s of silence; transfers to the front desk'.
2. Sam chooses Edit; the same inbound policy sheet as Go live (P0.9) opens.
3. Sam sets max duration and the silence limit in seconds.
4. Sam turns on the end rules needed from four: conversation complete, caller asks to end, fax, another AI assistant answers.
5. Sam adds a transfer number in E.164 with a short description and saves; the row summary updates.

### Rainy paths (each needs its own state in the prototype and a frame in Figma)
*   **P3.3.b** Every rule is off by default in the spec: The sheet pre-fills a recommended set and says which rules stay off.
*   **P3.3.c** Sam thinks in seconds, the API stores milliseconds: Sam types the silence limit in seconds; Studio saves max_silence_duration_ms and names the API field in a tooltip.
*   **P3.3.d** The transfer number is not E.164, or the description passes 1000 characters: Inline error on that field; Save waits.
*   **P3.3.e** Sam changes the policy while sessions are running: The sheet says the change applies to new sessions (API team to confirm).
*   **P3.3.f** A value is out of range: Inline error with the allowed range from the spec, before Save.
*   **P3.3.g** Sam needs the transfer headers or transfer type the current Console had: Not in the spec: the sheet leaves them out, says so once, and they are raised as an API ask.
*   **P3.3.h** The number is only a run's from number: Inbound policy never applies to runs; the sheet says a run's end rules are set on the run.
*   **P3.3.i** Sessions keep hitting the time cap: Once session_ended carries the number (G2), the row shows how many sessions ended at the cap in 7 d and links to them in history.

## Goal (KPI)
**In one line:** Fewer than 5 in 100 inbound sessions get cut off by the time cap.
*   Metric: Cut-off inbound sessions
*   Target: ≤ 5 %
*   How we measure: Inbound sessions whose duration ends within 2 s of max_call_duration_seconds, share of inbound sessions per number per week (session_cut_off, derived). Blocked on G2 (session_ended with a number id), G14 and G10 (the policy in force at session time).
*   Counter-metric: Production sessions under 45 s ≤ 15 %
*   Assumption we are testing: Sam sets the policy once, at Go live: ≥ 80 % of policy saves come from Go live. Method: operation_succeeded {telephony_phone_number_update} by entryPoint (entryPoint is on the allowlist). Kill: above 40 % from Numbers makes Numbers the primary door.

## Telemetry to configure before this ships
| Event | Fired by | Status |
| --- | --- | --- |
| `call_policy_opened {entryPoint: numbers_page|go_live}` | client (PostHog) | new client event (PostHog) |
| `operation_succeeded {operation: telephony_phone_number_update, entryPoint: numbers_page|go_live}` | client (PostHog) | in 2.1.0 |
| `session_ended (G2, no owner)` | server | server, API gateway emitter |
| `session_cut_off (derived, needs G2, G14, G10)` | server | server, API gateway emitter |

**Funnel stages this moves:** 10 agent_answered_production (derived; guards the idle threshold)
**Server gaps that block the read:** G2, G10, G14 (see the sheet, Server gaps)
**Where it is read:** PostHog (client), API gateway emitter (server), cohort read 14 d after the week. KPI owner: v3 mapping umbrella.

## Research
| Competitor | Status | Note |
| --- | --- | --- |
| Vapi | Done | Product and Docs. vapi-05-call-timeouts.png (silence timeout and max duration sliders), vapi-05-idle-messages.png, vapi-09-deprecated-settings.png (forwarding number) in competitors/product/vapi; marked/vapi-05-voicemail-detection.png. Set on the assistant, not the number. |
| Retell | Partial | Product and Docs. competitors/product/retell/retell-agent-editor.png shows a Call Settings accordion, collapsed; competitors/marked/retell-05-handle-voicemail.png (hang up or leave a message). Max duration, silence end and transfer not captured. |
| ElevenLabs | Done | Product. competitors/product/elevenlabs/elevenlabs-05-conversation-limits.png: turn after silence, end after silence, max duration and the line spoken at the cap, on the agent's Settings (tracker-board.json row 05). |
| LiveKit | Partial | Product and Docs. Call ending sits behind Configure in competitors/product/livekit/livekit-19-agent-builder-conversation.png, not opened; competitors/marked/livekit-05-session-events.png: silence handled in code. No number-level policy. |

**Still owed:** Open LiveKit Call ending Configure and Retell Call Settings. Inbound settings at each vendor: max duration, silence timeout, end rules, transfer, where they live and their defaults. Learning: every vendor puts these on the agent; v3 puts them on the number, so one sheet opens from Go live and from the number row and reads the same in both.

## Deliverables (filled by the agent, locked by Shakti)
*   Prototype (opens at the start of Sam's journey): after the run
*   Figma flow, page "v3 · P3 v3 mapping", section "P3.3 · Decide how sessions on numbers end": after the run
*   Lock: commit on design/v3 once approved
*   Sheet row: https://claude.ai/artifact/6YZCRsJvpoBthj4fnm9XAb
*   Research folder: references/v3/features/P3.3/

## Agent brief (read before designing)
**Scope (sub-tasks):**
- [ ] Reuse the P0.9 inbound policy sheet
- [ ] Policy summary line on the number row
- [ ] Four end rules, spec names in tooltips
- [ ] Seconds in UI; silence saved as ms
- [ ] Cut SIP transfer headers and type
- [ ] Range errors from spec bounds (new)
- [ ] Run-only number note in the sheet (new)
- [ ] Cut-off count on the row once G2 ships (new, blocked on G2)
**API:** Ready: InboundCallPolicy: max_call_duration_seconds, max_silence_duration_ms, end_call (4 flags: on_conversation_complete, on_user_request, on_fax, on_ai_assistant), transfer. Missing: transfer headers and type (current Console UI-only) and lifecycle on inbound.
**Register rows:** 48. Scope: all the team's numbers; one agent's first number is P0.9.
**Journeys:** IN · Inbound in Studio · ST · Stalled at Go live · FX · Fix loop
**Design days budgeted:** 0.25
**Rules:** existing Console design system only (docs/design/DESIGN.md on design/v3); reuse, do not redesign; empty first, quiet chrome; sentence case, no arrows or em dashes in prose; locked words from the sheet vocabulary; the persona is Sam and appears only in job text; never change a locked feature unless in scope, then say so in the review post.
**Output of one run:** 1 prototype on design/v3 reachable by URL for the happy path and every rainy state · 2 one screenshot per step of the happy path and per rainy state · 3 the Figma flow: frames in step order, each captioned "Sam does …" with the rationale under it, research shots marked in red · 4 this task moved to in review with a comment (prototype, Figma, commit, rationale) · 5 the sheet row updated · 6 the Slack post "JTBD n of 42 · <id> <title> is done and pending your review", prototype link first, then Figma, then 3 to 5 rationale lines, then open questions.