🏷 **V3 · launch Fri 16 Oct 2026** · P0 Agent config · P0.4 · design ETA **Sun 27 Sep** · lock ETA **Tue 29 Sep** · sheet status WIP

**Status is the queue.** Open = planned · in progress = the design agent takes it at the next run (08:00, 11:59, 16:00, 20:00) · in review = delivered, pending Shakti's review · completed = approved and locked (Figma section frozen, commit recorded) · on hold = parked. To ask for changes: move it back to in progress and comment `change: …`.

## Job to be done (Sam, a developer)
**Job:** Tell the agent its job · job map stage **Define** (Sam decides what the job needs before doing it)
**Umbrella job (P0):** Sam wants to create a new agent that people outside the team can talk to.
**Situation:** Sam tells the agent who it is and what to do, what it says first, and what it says when something fails.
**Sam wants to:** the agent to handle a real task
**So that:** callers get the job done in one session

### P0.4.a Happy path (this is what Sam does)
1. Prompt & knowledge opens with the prompt editor first.
2. Sam writes the prompt. {{variables}} show as chips, each with an optional default.
3. Sam opens Greeting to set the first line, when it plays, its delay and the failure message.
4. Sam presses Save and test.
5. Optional: Sam says what a successful session looks like in Analysis (P1.9).

### Rainy paths (each needs its own state in the prototype and a frame in Figma)
*   **P0.4.b** Sam clears the prompt: Save is blocked, with one line explaining why. One action restores the preset prompt.
*   **P0.4.c** A {{variable}} has no default: The test asks for a value once. For batch, the variable maps to a list column at New run.
*   **P0.4.d** No failure message is set: A tooltip says the agent stays silent when the language model fails. One click adds a suggested line.
*   **P0.4.e** The agent speaks first but the greeting is empty: Save names the empty line. Sam writes one or switches to caller first.
*   **P0.4.f** Sam tests with unsaved prompt edits: The test saves first, so Sam never hears an old prompt.

## Goal (KPI)
**In one line:** Median time to first prompt change under 2 minutes.
*   Metric: Time to first prompt change
*   Target: Median 2 active min or less from builder_opened to the first prompt or greeting save. Details: at least 80 % of agent_created {source console} reach agent_configured within 14 d (funnel read).
*   How we measure: builder_opened to the first prompt_edited or greeting_configured followed by agent_updated with fields instructions|greeting, active median. Funnel read: agent_created {source console} to agent_configured within 14 d, share.
*   Counter-metric: Prompt churn (3 or more prompt_edited with no heard test between them) in 20 % of builder sessions or fewer
*   Assumption we are testing: Putting the prompt first gets people configured faster. Method: compare with a 2-week baseline on the current builder once events are ported. Kill if the median does not drop by 20 % or more against the W0 baseline.

## Telemetry to configure before this ships
| Event | Fired by | Status |
| --- | --- | --- |
| `prompt_edited (port 1.0.0)` | client (PostHog) | new client event (PostHog) |
| `greeting_configured (renames opening_configured)` | client (PostHog) | new client event (PostHog) |
| `builder_opened (port 1.0.0)` | client (PostHog) | new client event (PostHog) |
| `agent_updated` | server | server, API gateway emitter |
| `agent_configured (derived)` | server | server, API gateway emitter |

**Funnel stages this moves:** 5 agent_configured (derived; instructions, greeting)
**Where it is read:** PostHog (client), API gateway emitter (server), cohort read 14 d after the week. KPI owner: Agent config umbrella.

## Research
| Competitor | Status | Note |
| --- | --- | --- |
| Vapi | Done | Product. competitors/product/vapi/vapi-assistant-model.png shows First Message with 'Assistant speaks first' above the System Prompt editor. |
| Retell | Done | Product. competitors/product/retell/retell-agent-editor.png shows the prompt leading, with Welcome Message ('AI speaks first') and pause before speaking. |
| ElevenLabs | Done | Product. competitors/product/elevenlabs/elevenlabs-agent-agent.png shows System prompt with {{ variables, First message with Interruptible, and a disclosure link. |
| LiveKit | Done | Product and Docs. competitors/product/livekit/livekit-19-agent-builder-conversation.png shows Instructions with Insert variable and a Welcome message with an interrupt toggle. Also public-docs/livekit-docs-speech-greeting.png. |

**Still owed:** No vendor shot shows where the failure message lives. Capture it, plus an empty-prompt save and a variable with no value at test. Own before and after shots: research/04-greeting-filler/05-shots.

## Deliverables (filled by the agent, locked by Shakti)
*   Prototype (opens at the start of Sam's journey): after the run
*   Figma flow, page "v3 · P0 Agent config", section "P0.4 · Tell the agent its job": after the run
*   Lock: commit on design/v3 once approved
*   Sheet row: https://claude.ai/artifact/6YZCRsJvpoBthj4fnm9XAb
*   Research folder: references/v3/features/P0.4/

## Agent brief (read before designing)
**Scope (sub-tasks):**
- [ ] Prompt editor as the section's first block
- [ ] Greeting sheet: text, who speaks first, delay, failure message
- [ ] Variable chips with an optional default value
- [ ] Empty prompt, empty greeting and missing failure message states
**API:** Ready: instructions, greeting {mode, on, delay_ms, text}, llm.failure_message, variables.
**Register rows:** 5, 9, 11, 40. Cross-link: P1.9 defines what a good session looks like.
**Journeys:** B · Build loop · INT · Integration · IN · Inbound in Studio · BA · Batch run in Studio · CO · Code in Studio
**Design days budgeted:** 0.125
**Rules:** existing Console design system only (docs/design/DESIGN.md on design/v3); reuse, do not redesign; empty first, quiet chrome; sentence case, no arrows or em dashes in prose; locked words from the sheet vocabulary; the persona is Sam and appears only in job text; never change a locked feature unless in scope, then say so in the review post.
**Output of one run:** 1 prototype on design/v3 reachable by URL for the happy path and every rainy state · 2 one screenshot per step of the happy path and per rainy state · 3 the Figma flow: frames in step order, each captioned "Sam does …" with the rationale under it, research shots marked in red · 4 this task moved to in review with a comment (prototype, Figma, commit, rationale) · 5 the sheet row updated · 6 the Slack post "JTBD n of 42 · <id> <title> is done and pending your review", prototype link first, then Figma, then 3 to 5 rationale lines, then open questions.