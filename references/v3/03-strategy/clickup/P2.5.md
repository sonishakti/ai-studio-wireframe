🏷 **V3 · launch Fri 16 Oct 2026** · P2 Monitoring · Sessions · design ETA **Sun 4 Oct** · lock ETA **Tue 6 Oct**

## Job to be done (Sam, a developer)
**Job:** Follow and stop a run · job map: Monitor
**Umbrella job (P2):** Sam wants to find out what happened in a session, or in a set of sessions, and why.
**Situation:** While the agent dials a list, Sam sees whether it is going well and stops it before more people get a bad session.
**Story:** Sam started a run and wants to know how it is going, so Sam can stop it early if contacts are failing.

### P2.5.a Happy path: what Sam does
1. Sam opens the agent's Runs tab (/agents/:id/runs) and sees each run with status and a counts bar.
2. The run page (/runs/:runId) shows calling windows, the agent chip, from-number chips and counts.
3. Calling and completed counts open the run's sessions once linked; failed opens contacts and attempts once contacts ship; pending and canceled read 'Not dialled'.
4. Sam pauses, resumes or cancels; Run again opens a new run prefilled with the same contact list.
5. The run's sessions show in the shared list, scoped 'Run: <name>'.

### Rainy paths: when it goes wrong
*   **P2.5.b** Sam cancels a run: The confirm names how many pending contacts will not be dialled.
*   **P2.5.c** The run failed: Code, provider and message, with Run again and a link to the error group.
*   **P2.5.d** The run is paused with no reason given: Reads 'Paused' without a reason until the API adds one.
*   **P2.5.e** Sessions cannot be linked to the run yet: Counts show; segments are disabled with the reason.
*   **P2.5.f** The agent was deleted: Cached name, or 'Deleted agent (agent_…)'.
*   **P2.5.g** The run is scheduled but not started, or outside its calling window: The header shows the next calling window; no sessions yet.
*   **P2.5.h** Free minutes run out mid-run: The minutes banner says the account is suspended and dialling stopped; pending contacts stay pending, and Run again works after top-up.
*   **P2.5.i** A failed dial may not create a session: Failed opens contacts and attempts once contacts ship; until then it is disabled with the reason.
*   **P2.5.j** Pause or cancel fails on a network or API error: The control returns to its prior state with the error and a retry.
*   **P2.5.k** Run not found or no access: A not-found state with a link to Agents.

## Goal (KPI)
**6 in 10 bad runs get paused or canceled before half the contacts are dialled.**
*   Metric: Bad runs stopped early
*   Target: ≥ 60 %
*   How we measure: A Studio job polls GET /campaigns counts every 5 min. Bad run: failed / (completed + failed) of 20 % or more at any point before 50 % progress. Numerator: run_status_changed {to: paused|canceled, trigger: user} before progressShare 0.5. Pauses from suspension or the API are excluded. Share of bad runs.
*   Counter-metric: Pauses undone within 5 min ≤ 10 %
*   Assumption: Counts plus pause stop bad runs early; the average Sam checks a new run within its first 10 minutes. Kill below 40 %.

## Telemetry
| Event | Fired by | Status |
| --- | --- | --- |
| `run_opened {status, entryPoint, failedBucket}` | client (PostHog) | new client |
| `run_status_changed {to, trigger, progressShare, failedShare}` | client (PostHog) | new client |
| `run_again_clicked` | client (PostHog) | new client |
| `monitoring_pivot {fromLevel: run}` | client (PostHog) | new client |
| `operation_succeeded {operation: telephony_campaign_interrupt}` | client (PostHog) | in 2.1.0 |
| `campaign_counts_snapshot (derived, Studio poll every 5 min)` | server | server |

Funnel stages this moves: 10 agent_answered_production (derived; batch, provisional) · 12 session_reviewed (surface run)
Server gaps that block the read: G4, G12

## Research
| Competitor | Status | Note |
| --- | --- | --- |
| Datadog | Not started | Not applicable: no run object; not needed for this step. |
| LiveKit | Done | Docs, absent. No batch product; per-session dispatch only (competitors/public-docs/livekit-18m-outbound-calls-agent-dispatch.png). Same rule as P0.10. |
| Vapi | Partial | Desk. Outbound Campaigns metrics: audience, remaining, connected, pick-up rate (batch-retention-experiments.md); no shot. |
| Retell | Partial | Product. Create form only: retell-18-batch-call-form.png; batch progress blocked at login: retell-18m-batchcall-login-blocked.png (competitors/product/retell). |
| ElevenLabs | Partial | Product. Empty batch list and form: elevenlabs-18m-batch-list-empty.png, elevenlabs-18-batch-call-form.png (competitors/product/elevenlabs); a running batch not captured. |
| Bland | Partial | Desk. Batch lifecycle incl. 'Completed Partial' and batch-filtered call logs (competitor-monitoring.md). |

Still owed: Shoot a running batch in Retell and ElevenLabs (progress, pause, cancel) and a Vapi campaign page; ask each vendor how a failed dial is recorded.

## Deliverables
*   Prototype: not yet
*   Figma flow: not yet (page v3 · P2 Monitoring · Sessions, section P2.5 · Follow and stop a run)
*   Commit: not yet
*   Sheet row: https://claude.ai/artifact/6YZCRsJvpoBthj4fnm9XAb#P2.5

---

###### Footnote for the design agent
Scope: Runs tab row; Run header and chip lists; Contact-state to session-status table; Pause, resume, cancel, Run again; States incl. scheduled, suspended mid-run, not found; Counts poll job for the KPI.
API: Ready: GET /campaigns/{id}, pause, resume, cancel. Missing: sessions by run (G4, G12); contacts endpoint; pause reason (decision 16). Open: does a failed dial create a Session? Register: 7, 42; 8 and 47 (partial). Re-home beside P1.8 proposed; owner approval needed. Journeys: BA · Batch run in Studio · RK · Free minutes run out · ST · Stalled at Go live · FX · Fix loop. Days: 0.375.
Rule 0, data first. This flow shows: production sessions, error groups and log lines, phone numbers, batch runs and contact lists, billing, free minutes, a card, several agents. New account: A new account has no sessions to open and nothing to filter. Session detail, turns and logs also depend on server gaps G2, G4, G11, G13. Outside our accounts: Retell call history and LiveKit sessions (signed-in), Langfuse and LangSmith docs for trace timelines and filters, Twilio event streams; the 61 monitoring shots already captured. Create in our account: In our account: the same 10 test sessions as P1 plus 1 session with zero retention and 1 with a long silence, so history shows a failed, a zero-retention and a slow session side by side.
Files: references/v3/features/P2.5/ (jtbd, directions, build spec, flow/ screenshots, shots/ research).
Status: Open = planned · in progress = taken at the next run (08:00, 11:59, 16:00, 20:00) · in review = pending Shakti · completed = locked (Figma frozen, commit) · on hold = parked. Changes: back to in progress + comment `change: …`.
Rules: existing Console design system only (docs/design/DESIGN.md, design/v3); reuse, do not redesign; empty first, quiet chrome; sentence case, no arrows or em dashes; locked words; Sam only in job text; never change a locked feature unless in scope, then say so. One run delivers: prototype for the happy path and every rainy state, one screenshot per step, the Figma flow with "Sam does …" captions and rationale, this task in review with a comment, the sheet row, one Slack post.