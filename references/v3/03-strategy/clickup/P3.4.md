🏷 **V3 · launch Fri 16 Oct 2026** · P3 v3 mapping · design ETA **Wed 7 Oct** · lock ETA **Fri 9 Oct**

## Job to be done (Sam, a developer)
**Job:** Reuse integrations across agents · job map: Prepare
**Umbrella job (P3):** Sam wants to look after everything the team's agents share, by hand or from code, and get the same result either way.
**Situation:** Sam gives several agents the same access to another system and knows which agents a change would affect.
**Sam wants to:** the CRM lookup tool from the support agent on the sales agent too
**So that:** both answer from the same data

### P3.4.a Happy path: what Sam does
1. Sam opens Integrations; every MCP server and tool found on the project's agents is listed once, grouped by endpoint or URL.
2. Each row shows name, type icon, allowed tools and used by.
3. Sam chooses Add to agent on the CRM tool and picks the sales agent.
4. Studio copies the entry into that agent's Integrations list and asks Sam to re-enter the header values.
5. Sam saves and tests the sales agent; app_connected fires if this is its first integration.

### Rainy paths: when it goes wrong
*   **P3.4.b** Headers do not come across in the copy: Headers are write-only, so Add to agent asks Sam to re-enter each value, or pick a secret reference once the API confirms $secrets works in headers.
*   **P3.4.c** Sam looks for a knowledge base or HubSpot: Not in the v3 spec: no row ships that cannot save; items from the current Console show read-only with a notice.
*   **P3.4.d** Sam looks for WhatsApp among integrations: WhatsApp leaves this list: it is a future deployment type and transport, never an integration.
*   **P3.4.e** Sam has no integrations yet: The empty state opens the builder's Integrations list.
*   **P3.4.f** Sam changes one copy and expects every agent to change: Each agent holds its own copy; there is no project-level object. The row says a change on one agent stays on that agent and flags agents whose allowed tools or timeout differ.
*   **P3.4.g** An integration fails and the row cannot say so: The API has no health field, so the row shows no status; used by links open each agent's test (journey INT-R).
*   **P3.4.h** The project has many agents and the scan is slow or fails: The page scans GET /agents page by page and fills rows as they load; on a failure it keeps what loaded and offers Retry.

## Goal (KPI)
**3 in 10 accounts with a second agent reuse an MCP server or tool within 30 days.**
*   Metric: Reuse rate
*   Target: ≥ 30 % of accounts reaching a second agent reuse one MCP endpoint or tool URL on 2 or more agents within 30 d. Details: the page's share of reuse events ≥ 15 %.
*   How we measure: Accounts reaching a second agent_created; within 30 d of it, the same MCP endpoint or tool URL is on 2 or more agents (GET /agents scan), share. Page attribution: integration_added {entryPoint: integrations_page}, share of reuse events.
*   Counter-metric: Integrations removed within 24 h ≤ 15 %
*   Assumption: Sam reuses integrations across agents, and the page helps. Method: GET /agents scan at 30 d. Kill: if the page's share of reuse events is below 15 %, retire the page and keep integrations in the builder.

## Telemetry
| Event | Fired by | Status |
| --- | --- | --- |
| `page_viewed {productArea: integrations}` | client (PostHog) | in 2.1.0 |
| `operation_succeeded {operation: integration_attach}` | client (PostHog) | in 2.1.0 |
| `integration_used_by_opened {integrationType, usedByCount}` | client (PostHog) | new client |
| `integration_added {entryPoint: integrations_page}` | client (PostHog) | new client |
| `agent_created` | server | server |
| `app_connected` | server | server |

Funnel stages this moves: 7 app_connected

## Research
| Competitor | Status | Note |
| --- | --- | --- |
| Vapi | Partial | Product and Desk. A workspace Tools list reused across assistants (research/19-tools-connectors/02-research/_docs.md). vapi-assistant-advanced.png shows Tools and Integrations in the rail, not the list. |
| Retell | Done | Product. retell-19-integrations-available.png, retell-19-agent-functions-section.png, retell-20-kb-source-types.png in competitors/product/retell. |
| ElevenLabs | Done | Product. elevenlabs-19-tools-list.png, elevenlabs-19-integrations-mcp-library.png, elevenlabs-19-custom-mcp-server-form.png, elevenlabs-19-webhook-tool-form.png, elevenlabs-20-knowledge-base-list.png. Nothing shows which agents use a tool (report-13-22.md §19). |
| LiveKit | Done | Product, absent. livekit-19-agent-builder-actions.png, livekit-19-http-tool-form.png, livekit-19-mcp-server-form.png: per agent only, no library (report-13-22.md §19). |

Still owed: Tool and MCP libraries at each vendor: shared or per agent, used by, knowledge base states, header handling. Learning: Vapi and ElevenLabs keep a workspace list, LiveKit attaches per agent only, and none shows used by before a change, which is why this page exists.

## Deliverables
*   Prototype: not yet
*   Figma flow: not yet (page v3 · P3 v3 mapping, section P3.4 · Reuse integrations across agents)
*   Commit: not yet
*   Sheet row: https://claude.ai/artifact/6YZCRsJvpoBthj4fnm9XAb#P3.4

---

###### Footnote for the design agent
Scope: Keep /integrations; four tabs become one list; Scan GET /agents; group by endpoint or URL; Row: name, type icon, allowed tools, used by; Add to agent: copy entry, re-enter headers; Knowledge base and apps: read-only notice; Remove WhatsApp from the catalog; Empty state opens builder Integrations list; Paged scan with progress and Retry (new); Row note: a change stays on one agent; flag differing copies (new).
API: Partial. No project-level integration object: derived from GET /agents; headers write-only. Missing: knowledge base, app integrations, health or execution log. Register: 10 (partial), 55 (new). Scope: across agents; one agent's integrations are P0.5. Journeys: INT · Integration · B · Build loop · API · API-only, saved agent · FX · Fix loop. Days: 0.25.
Rule 0, data first. This flow shows: error groups and log lines, secrets and provider keys, integrations (MCP servers, tools), several agents. New account: A new account has one project, no numbers, no secrets and no integrations. Lists open empty; reuse and replace flows need at least two items. Outside our accounts: Vapi and Retell signed-in accounts for numbers and keys pages; ElevenLabs for tools and MCP; the v3 API spec for every field. Create in our account: 2 secret sets (one referenced by an agent), 3 numbers (one unassigned for 7 days), 2 MCP servers shared by 2 agents, 1 API-created agent to test code and hand edits.
Files: references/v3/features/P3.4/ (jtbd, directions, build spec, flow/ screenshots, shots/ research).
Columns: To-do = planned, in the order it gets done · In progress = taken at a run (08:00, 11:59, 16:00, 20:00) · Pending Review = delivered, pending Shakti · Delivered = approved and locked (Figma frozen, commit). Changes: drag back to In progress + comment `change: …`.
Rules: existing Console design system only (docs/design/DESIGN.md, design/v3); reuse, do not redesign; empty first, quiet chrome; sentence case, no arrows or em dashes; locked words; Sam only in job text; never change a locked feature unless in scope, then say so. One run delivers: prototype for the happy path and every rainy state, one screenshot per step, the Figma flow with "Sam does …" captions and rationale, this card in Pending Review with a comment, the sheet row, one Slack post.