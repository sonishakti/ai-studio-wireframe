🏷 **V3 · launch Fri 16 Oct 2026** · P0 Agent config · design ETA **Sun 27 Sep** · lock ETA **Tue 29 Sep**

## Job to be done (Sam, a developer)
**Job:** Make the conversation feel natural · job map: Prepare
**Umbrella job (P0):** Sam wants to create a new agent that people outside the team can talk to.
**Situation:** When the agent is close but not right, Sam makes the conversation feel natural: it stops talking over people, waits the right time through a silence, and still says something useful when a part fails.
**Sam wants to:** the agent to stop talking over callers
**So that:** conversations sound natural and callers stay on the line

### P0.3.a Happy path: what Sam does
1. Sam opens Advanced from Voice & models. It is one panel, grouped Listen, Think, Speak.
2. Each field shows its spec default, with a tooltip instead of helper text.
3. Sam changes turn-taking, silence or filler words. The agent stays on its preset unless a model changes.
4. Sam presses Save and test and hears the change in the next answer.

### Rainy paths: when it goes wrong
*   **P0.3.b** Sam enters a value out of range (silence_duration_ms 120 to 2000): The field error states the range.
*   **P0.3.c** Sam changes a model while on a preset: The agent becomes Custom, and one line says why.
*   **P0.3.d** The agent runs a realtime pipeline: The ASR, TTS, silence and tool groups are hidden with the reason. MCP servers stay. server_vad idle_timeout_ms shows, labelled OpenAI Realtime only.
*   **P0.3.e** Sam looks for idle timeout, max duration or graceful stop: These are session settings, not agent fields. One line points to the code snippet and New run, where they live.
*   **P0.3.f** Sam regrets a change: Reset returns that group to spec defaults.
*   **P0.3.g** An API agent uses a field the panel lacks: The field shows read-only with its value and is never dropped on save.
*   **P0.3.h** Sam leaves with unsaved changes: A guard asks Sam to save or discard.

## Goal (KPI)
**6 in 10 advanced changes are heard in the same session.**
*   Metric: Advanced change heard
*   Target: At least 60 % of Advanced changes are followed by a heard test answer in the same builder session.
*   How we measure: advanced_setting_changed to agent_audio_heard on the same agent in the same builder session, as a share of advanced_setting_changed
*   Counter-metric: Advanced opened by 30 % of new agents or fewer before channel_connected
*   Assumption: Spec defaults fit most agents: at most 30 % of new agents open Advanced before Go live. Method: cohort read. Above 50 %, move the most-changed group out of Advanced.

## Telemetry
| Event | Fired by | Status |
| --- | --- | --- |
| `advanced_panel_opened` | client (PostHog) | new client |
| `advanced_setting_changed` | client (PostHog) | new client |
| `advanced_group_reset` | client (PostHog) | new client |
| `model_slot_configured (port 1.0.0)` | client (PostHog) | new client |
| `agent_audio_heard (port 1.0.0 + new props surface, agentVersion; agentVersion needs an allowlist key)` | client (PostHog) | new client |
| `operation_succeeded {operation: agent_update}` | client (PostHog) | in 2.1.0 |
| `agent_updated` | server | server |

Funnel stages this moves: 5 agent_configured (derived; model fields only, tuning alone never counts)

## Research
| Competitor | Status | Note |
| --- | --- | --- |
| Vapi | Done | Product and Docs. competitors/product/vapi/vapi-assistant-advanced.png, vapi-05-idle-messages.png, vapi-05-call-timeouts.png and vapi-03-fallback-transcriber.png. Also public-docs/vapi-docs-speech-configuration.png. |
| Retell | Done | Product and Docs. competitors/product/retell/retell-agent-editor.png shows the Speech, Call and Security & Fallback settings accordions. Also public-docs/retell-docs-basic-settings.png and retell-03-background-noise.png. |
| ElevenLabs | Done | Product and Docs. competitors/product/elevenlabs/elevenlabs-05-agent-settings.png, elevenlabs-05-conversation-limits.png and elevenlabs-06-voice-tuning.png. Also public-docs/elevenlabs-docs-llm-fallback.png. |
| LiveKit | Done | Docs. competitors/public-docs/livekit-docs-turn-detector.png and competitors/public-docs/livekit-05-session-lifecycle.png. The product shots cited before (livekit-15 console sheet, livekit-18 advanced telephony) show no conversation settings and are dropped. |

Still owed: Open the LiveKit builder turn and interruption settings in the product. No vendor shot shows reset to default or an out-of-range error; capture one each. Desk notes: v3/02-research/builder-models-secrets.md §A (ElevenLabs grouping as a completeness checklist). Older briefs: research/02-turn-taking, research/05-call-behavior. Own shots: research/04-greeting-filler/05-shots.

## Deliverables
*   Prototype: not yet
*   Figma flow: not yet (page v3 · P0 Agent config, section P0.3 · Make the conversation feel natural)
*   Commit: not yet
*   Sheet row: https://claude.ai/artifact/6YZCRsJvpoBthj4fnm9XAb#P0.3

---

###### Footnote for the design agent
Scope: Port AdvancedSharedSection, do not redraw; Group order Listen, Think, Speak; Default per field; tooltip, not helper text; Add missing agent fields: avatar, semantic start, headers, url; Read-only row for API fields the panel lacks; Realtime variant: hide groups mllm lacks; Per-group reset, range errors and leave guard.
API: Ready: turn_detection, silence_config, filler_words, max_history, failure_message and avatar on the agent. Missing: lifecycle exists only on SessionCreate and POST /campaigns. Register: 38, 11 Journeys: B · Build loop · API · API-only, saved agent · FX · Fix loop. Days: 0.25.
Rule 0, data first. This flow shows: production sessions, turns, transcript, recording, error groups and log lines, batch runs and contact lists, integrations (MCP servers, tools). New account: A new account has no agent and no session; the flow creates them. Presets, voices and provider lists exist without data. Outside our accounts: Retell and ElevenLabs signed-in accounts (competitor profile) already hold configured agents; LiveKit and Vapi docs for create and test flows; Refero for create sheets and test panels. Create in our account: In our account: 1 agent per deployment type (inbound, batch, code), 1 secret set with one wrong key to reach the failure state, 2 test sessions (one heard, one failed).
Files: references/v3/features/P0.3/ (jtbd, directions, build spec, flow/ screenshots, shots/ research).
Status: Open = planned · in progress = taken at the next run (08:00, 11:59, 16:00, 20:00) · in review = pending Shakti · completed = locked (Figma frozen, commit) · on hold = parked. Changes: back to in progress + comment `change: …`.
Rules: existing Console design system only (docs/design/DESIGN.md, design/v3); reuse, do not redesign; empty first, quiet chrome; sentence case, no arrows or em dashes; locked words; Sam only in job text; never change a locked feature unless in scope, then say so. One run delivers: prototype for the happy path and every rainy state, one screenshot per step, the Figma flow with "Sam does …" captions and rationale, this task in review with a comment, the sheet row, one Slack post.