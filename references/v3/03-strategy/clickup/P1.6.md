🏷 **V3 · launch Fri 16 Oct 2026** · P1 Monitoring · Agents · P1.6 · design ETA **Thu 1 Oct** · lock ETA **Mon 5 Oct** · sheet status Planned

**Status is the queue.** Open = planned · in progress = the design agent takes it at the next run (08:00, 11:59, 16:00, 20:00) · in review = delivered, pending Shakti's review · completed = approved and locked (Figma section frozen, commit recorded) · on hold = parked. To ask for changes: move it back to in progress and comment `change: …`.

## Job to be done (Sam, a developer)
**Job:** Remove the cause · job map stage **Modify** (Sam adjusts without breaking what runs)
**Umbrella job (P1):** Sam wants to know how each agent is doing with people outside the team, and fix what fails before customers notice.
**Situation:** Sam removes the cause of a failure and confirms the failure has stopped.
**Story:** A log line says speech recognition timed out, and Sam wants to change that setting without hunting through the builder, then check the error stops.

### P1.6.a Happy path (this is what Sam does)
1. On a log line, slow turn or error group, Sam clicks 'Open the setting'.
2. The builder opens on the mapped section with the field focused: turn detection, ASR, LLM and TTS to Voice & models (Advanced, that module); a tool to its integration row; telephony to the number or run; greeting or instructions to Prompt; a rejected key to the secret set Studio recorded.
3. One 'Back to <source>' chip sits at the top.
4. Sam saves; the builder offers 'Sessions since this change with this error' and 'Analytics since this change'.
5. Sam clicks the chip and returns to the same line, range and filters.

### Rainy paths (each needs its own state in the prototype and a frame in Figma)
*   **P1.6.b** Agent was saved after the session: Banner: 'Agent saved after this session; you are editing the current version'.
*   **P1.6.c** Agent not linked, or the session was ephemeral: No door; one line says why.
*   **P1.6.d** Agent deleted: Door disabled with the reason.
*   **P1.6.e** Module unknown: Voice & models overview opens, never a guessed field.
*   **P1.6.f** Secret set unknown (agent created through the API): Door disabled with the reason.
*   **P1.6.g** Sam has no edit rights: Read-only view with the field highlighted.
*   **P1.6.h** Unsaved edits when Sam goes back: Ask Save or Discard before leaving.
*   **P1.6.i** The fix did not work: 'Sessions since this change with this error' lists new occurrences; the group is tagged Returned.
*   **P1.6.j** Sam goes two levels deep (builder, then secret set): The chip returns one level at a time; nesting stops at two.

## Goal (KPI)
**In one line:** 4 in 10 error line opens reach the builder within 10 minutes, median 1 step.
*   Metric: Root cause to setting
*   Target: ≥ 40 % of error log-line opens reach the builder within 10 min; median 1 step
*   How we measure: log_line_opened {severity: error} or turn_selected on a failed leg, to builder_opened {entryPoint: log_line|turn|error_group} on the same agent, linked by id (not by name), within 10 min, share. Right landing = 1 minus the wrong-landing counter.
*   Counter-metric: Wrong landing (another builder_section_opened within 60 s) ≤ 20 %; agent_reverted within 24 h ≤ 10 %
*   Assumption we are testing: A one-step door from the failing line to its field raises fixes. Method: cohort read; kill below 20 %.

## Telemetry to configure before this ships
| Event | Fired by | Status |
| --- | --- | --- |
| `builder_opened {entryPoint, section, agentChangedSince} (port 1.0.0 + new entryPoint values log_line|turn|error_group|session_detail and new props section, agentChangedSince)` | client (PostHog) | new client event (PostHog) |
| `builder_section_opened (port 1.0.0)` | client (PostHog) | new client event (PostHog) |
| `monitoring_pivot {trigger: return_chip}` | client (PostHog) | new client event (PostHog) |
| `agent_updated` | server | server, API gateway emitter |
| `agent_reverted (derived)` | server | server, API gateway emitter |

**Funnel stages this moves:** 12 session_reviewed then agent_updated · 8 agent_tested_configured (first only)
**Server gaps that block the read:** G10 (see the sheet, Server gaps)
**Where it is read:** PostHog (client), API gateway emitter (server), cohort read 14 d after the week. KPI owner: Monitoring · Agents umbrella.

## Research
| Competitor | Status | Note |
| --- | --- | --- |
| Datadog | Partial | Desk. No log-to-config door exists; 'View in context' from v3/02-research/observability-patterns.md and monitoring-model.json learnings. |
| LiveKit | Done | Docs, absent. v3/02-research/monitoring/shots/livekit-01-turnbyturn-playback.png and livekit-03-transcript-traces-logs-tabs.png show session views with no link to agent config; monitoring-model.json learnings agree. |
| Vapi | Partial | Docs. v3/02-research/monitoring/shots/vapi-05-call-detail-reading.png shows the assistant name and version on the session header; whether it links to the editor is unconfirmed. |
| Retell | Done | Docs. v3/02-research/monitoring/shots/retell-02-call-detail-panel.png: a View In Test Playground button in call detail, a session-to-agent door, plus agent id and version on the header. |
| ElevenLabs | Done | Docs, absent. v3/02-research/monitoring/shots/elevenlabs-04-eval-results-in-conversation-history.png: results with no link back to their criterion (avoid). |
| Bland | Done | Docs. v3/02-research/monitoring/shots/bland-02-call-detail-transcript-logs.png and bland-03-testbed-from-log-entry.png: 'Edit node' from a response, a visible button. |
| Honeycomb | Done | Docs. v3/02-research/monitoring/shots/honeycomb-06-trace-click-to-query-reverse.png: reverse pivot from one field. |

**Still owed:** Owed: confirm whether Vapi's assistant name on the session header links to the editor, and open Retell's View In Test Playground. Question: how often the mapped section is the wrong one (the counter).

## Deliverables (filled by the agent, locked by Shakti)
*   Prototype (opens at the start of Sam's journey): after the run
*   Figma flow, page "v3 · P1 Monitoring · Agents", section "P1.6 · Remove the cause": after the run
*   Lock: commit on design/v3 once approved
*   Sheet row: https://claude.ai/artifact/6YZCRsJvpoBthj4fnm9XAb
*   Research folder: references/v3/features/P1.6/

## Agent brief (read before designing)
**Scope (sub-tasks):**
- [ ] Module-to-section map from observed values
- [ ] Field focus and highlight
- [ ] Return chip: same-origin only, unsaved-edit prompt, two-level nesting
- [ ] Saved-after banner and config-changed label
- [ ] Post-save confirmation doors
**API:** Partial. GET and PATCH /agents work; module_name on v2 events works; secret set from a Studio label only (credentials return mode only, decision 12); config at session time missing (G10).
**Register rows:** 60 (new); 16, 23. Scope: one failure; the overall trend is P1.3.
**Journeys:** FX · Fix loop · FX-R · Fix with nothing to read · INT-R · Integration saved, not working · CO-R · Code first session fails
**Design days budgeted:** 0.25
**Rules:** existing Console design system only (docs/design/DESIGN.md on design/v3); reuse, do not redesign; empty first, quiet chrome; sentence case, no arrows or em dashes in prose; locked words from the sheet vocabulary; the persona is Sam and appears only in job text; never change a locked feature unless in scope, then say so in the review post.
**Output of one run:** 1 prototype on design/v3 reachable by URL for the happy path and every rainy state · 2 one screenshot per step of the happy path and per rainy state · 3 the Figma flow: frames in step order, each captioned "Sam does …" with the rationale under it, research shots marked in red · 4 this task moved to in review with a comment (prototype, Figma, commit, rationale) · 5 the sheet row updated · 6 the Slack post "JTBD n of 42 · <id> <title> is done and pending your review", prototype link first, then Figma, then 3 to 5 rationale lines, then open questions.