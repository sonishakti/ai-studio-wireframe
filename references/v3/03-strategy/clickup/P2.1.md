🏷 **V3 · launch Fri 16 Oct 2026** · P2 Monitoring · Sessions · design ETA **Sat 3 Oct** · lock ETA **Mon 5 Oct**

## Job to be done (Sam, a developer)
**Job:** Spot sessions that went wrong · job map: Locate
**Umbrella job (P2):** Sam wants to find out what happened in a session, or in a set of sessions, and why.
**Situation:** Without a specific clue, Sam spots which recent sessions went wrong.
**Sam wants to:** see which recent sessions went wrong
**So that:** the worst one gets opened first

### P2.1.a Happy path: what Sam does
1. Sam opens Sessions, or the sessions block on an agent, a run, a number or an error group; every one shows the same session list.
2. The list starts at the last 7 days, all statuses, newest first; sessions Studio started carry a Test tag. Sam filters by status to bring failed sessions up.
3. Each row shows start, agent (a link only when an id proves it, else 'Agent not linked' with the name as text), status, then deployment type, direction, transport, duration, cost and error-group tag once the API returns them; tags Test, Ephemeral, Zero retention, Retention unknown.
4. Sam clicks a row and the session opens in a panel over the list (?session=&in=).
5. The up and down keys move to the next session; Esc closes the panel and the list is exactly as Sam left it.

### Rainy paths: when it goes wrong
*   **P2.1.b** The agent was deleted: The row keeps the agent name as text, with no link.
*   **P2.1.c** No id proves which agent ran the session: The row reads 'Agent not linked' with the name as text; never matched by name.
*   **P2.1.d** The API cannot filter the scope Sam came from: The list filters over loaded pages and says 'partial: first N pages'.
*   **P2.1.e** Nothing in the range: The empty state names the filter that emptied the list, with one-click clear or widen.
*   **P2.1.f** The session is still running: Duration counts up and cost reads pending.
*   **P2.1.g** The session source is down or the network drops: An error with retry; range and filters are kept.
*   **P2.1.h** Retention or origin not returned (G5): The row reads Retention unknown, never Zero retention; ephemeral sessions list like any other.
*   **P2.1.i** Tests crowd out production sessions: Every Studio-started session carries a Test tag, and a Production view is one click away.
*   **P2.1.j** Sam asks for a range past the lookback limit: Presets stop at 30 days until the limit is known; older ranges say 'Older than 30 days, not available'.
*   **P2.1.k** There are no sessions yet: The first-run state says why and offers Talk to agent and Go live (P2.7, P1.8).

## Goal (KPI)
**Median time to the session under 60 seconds, 30 percent faster than the current Console.**
*   Metric: Time to the session
*   Target: Median ≤ 60 s, and ≥ 30 % faster than the current two history pages in a moderated 5-owner task
*   How we measure: history_opened to session_opened, then link_copied, session_downloaded, talk_to_agent_started or agent_updated within 2 min, in the same visit (visitId, 30 min inactivity); median of history_opened to that session_opened over visits that reach it; split by entryPoint and scope
*   Counter-metric: Misses (session_opened, then left in < 5 s) ≤ 20 %
*   Assumption: One reused session list beats the two current history pages. The average Sam arrives and scans about 20 rows before opening one; kill the shared list if the task is not ≥ 30 % faster.

## Telemetry
| Event | Fired by | Status |
| --- | --- | --- |
| `history_opened {entryPoint, scope, env, rowCount, partial}` | client (PostHog) | new client |
| `session_opened {surface, origin, retention, agentSource}` | client (PostHog) | new client |
| `session_reviewed (renames call_reviewed)` | client (PostHog) | new client |
| `monitoring_pivot {fromLevel: sessions, trigger: row|keyboard|agent_cell}` | client (PostHog) | new client |
| `link_copied {object: session}` | client (PostHog) | new client |
| `session_downloaded` | client (PostHog) | new client |
| `page_viewed` | client (PostHog) | in 2.1.0 |

Funnel stages this moves: 12 session_reviewed (surface history)
Server gaps that block the read: G4, G5, G12

## Research
| Competitor | Status | Note |
| --- | --- | --- |
| Datadog | Done | Docs. Traces tab list pre-filtered by service, env and operation, with a facet sidebar: datadog-03 (monitoring/shots). |
| LiveKit | Done | Product. Sessions table with time range, filters and auto-refresh, empty 'No results.': livekit-sessions.png, livekit-12-sessions-live.png (competitors/product/livekit). Populated rows not captured. |
| Vapi | Done | Docs and Product. Calls table with filter row and Ended Reason: vapi-04 (monitoring/shots); vapi-logs.png, sip-calls-table-populated.png (competitors/product/vapi). |
| Retell | Done | Docs and Product. Call History with Agent ID, Name and Version columns and slide-over detail: retell-01, retell-02 (monitoring/shots); retell-call-history.png (competitors/product/retell). |
| ElevenLabs | Done | Product. Conversation history and its Customize table popover: elevenlabs-13-conversation-history-list.png, elevenlabs-13-history-customize-table.png, sip-conversations-list.png (competitors/product/elevenlabs). |
| Bland | Done | Docs. Call logs table with a Pathway link back to the agent: bland-01 (monitoring/shots). |

Still owed: Capture populated session lists while signed in (LiveKit with rows, Retell with 20+ rows) and how each vendor shows a session whose agent was deleted.

## Deliverables
*   Prototype: not yet
*   Figma flow: not yet (page v3 · P2 Monitoring · Sessions, section P2.1 · Spot sessions that went wrong)
*   Commit: not yet
*   Sheet row: https://claude.ai/artifact/6YZCRsJvpoBthj4fnm9XAb#P2.1

---

###### Footnote for the design agent
Scope: Row spec mapped to the API and v2; Agent column: linked, not linked, deleted; Tags incl. Retention unknown; Studio session registry (Console backend); Reuse on agent Overview, error group, run and number; Frames: default, loading, partial, narrow, source down, no sessions yet.
API: Partial: list agent_id appears to be the session id (decision 5). Missing: saved agent id, ended_at, cost, transport, direction, origin (G4, G5); filters (G12). Partial: v2 sources if decision 15 confirms. Register: 22, 23, 25, 26 (slot); 57 (new) Journeys: IN · Inbound in Studio · BA · Batch run in Studio · CO · Code in Studio · API · API-only, saved agent · EP · Ephemeral only · EP-R · Ephemeral session fails, nowhere to look · FX · Fix loop. Days: 0.375.
Rule 0, data first. This flow shows: production sessions, error groups and log lines, phone numbers, batch runs and contact lists. New account: A new account has no sessions to open and nothing to filter. Session detail, turns and logs also depend on server gaps G2, G4, G11, G13. Outside our accounts: Retell call history and LiveKit sessions (signed-in), Langfuse and LangSmith docs for trace timelines and filters, Twilio event streams; the 61 monitoring shots already captured. Create in our account: the same 10 test sessions as P1 plus 1 session with zero retention and 1 with a long silence, so history shows a failed, a zero-retention and a slow session side by side.
Files: references/v3/features/P2.1/ (jtbd, directions, build spec, flow/ screenshots, shots/ research).
Columns: To-do = planned, in the order it gets done · In progress = taken at a run (08:00, 11:59, 16:00, 20:00) · Pending Review = delivered, pending Shakti · Delivered = approved and locked (Figma frozen, commit). Changes: drag back to In progress + comment `change: …`.
Rules: existing Console design system only (docs/design/DESIGN.md, design/v3); reuse, do not redesign; empty first, quiet chrome; sentence case, no arrows or em dashes; locked words; Sam only in job text; never change a locked feature unless in scope, then say so. One run delivers: prototype for the happy path and every rainy state, one screenshot per step, the Figma flow with "Sam does …" captions and rationale, this card in Pending Review with a comment, the sheet row, one Slack post.