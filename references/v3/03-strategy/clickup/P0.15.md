🏷 **V3 · launch Fri 16 Oct 2026** · P0 Agent config · P0.15 · design ETA **not budgeted** · lock ETA **not budgeted** · sheet status Planned

**Status is the queue.** Open = planned · in progress = the design agent takes it at the next run (08:00, 11:59, 16:00, 20:00) · in review = delivered, pending Shakti's review · completed = approved and locked (Figma section frozen, commit recorded) · on hold = parked. To ask for changes: move it back to in progress and comment `change: …`.

## Job to be done (Sam, a developer)
**Job:** Retire an agent · job map stage **Conclude** (Sam wraps up or hands on)
**Umbrella job (P0):** Sam wants to create a new agent that people outside the team can talk to.
**Situation:** Sam retires an agent without leaving a number or run pointing at nothing. Proposed Conclude step, for owner sign-off; mark out of scope if preferred.
**Sam wants to:** remove an old agent
**So that:** no caller reaches a dead number and no run dials with a missing agent

### P0.15.a Happy path (this is what Sam does)
1. Sam chooses Delete on the agent.
2. A confirm lists every number and run that uses the agent, with a door to re-point or stop each.
3. Sam re-points the number to another agent and cancels the run.
4. Sam types the agent name and deletes. Its sessions and error groups stay in history, tagged Agent deleted (P1.4.g, P2.1.b).

### Rainy paths (each needs its own state in the prototype and a frame in Figma)
*   **P0.15.b** A number still points at the agent: Delete is blocked until the number is re-pointed or cleared (API team to confirm the server rule).
*   **P0.15.c** A scheduled or running run uses the agent: Delete is blocked; the confirm names the run and offers Pause or Cancel.
*   **P0.15.d** The agent's secret sets are used by no other agent: The confirm offers to retire those sets too; they are kept by default (P3.1).
*   **P0.15.e** The team's own code still starts sessions with this agent: Studio cannot see code references; the confirm says sessions started from code with this agent id will fail.
*   **P0.15.f** Sam deletes by mistake: There is no undo in the v3 spec, so the confirm asks Sam to type the agent name.

## Goal (KPI)
**In one line:** No number or run points at a deleted agent.
*   Metric: Orphaned deployments
*   Target: 0 numbers or runs that reference a deleted agent
*   How we measure: Daily scan of GET /numbers and GET /campaigns for agent ids missing from GET /agents (orphan_deployment, derived), count
*   Counter-metric: Deletes followed by a new agent with the same name within 24 h ≤ 5 %
*   Assumption we are testing: A delete confirm that lists every user of the agent prevents orphans. Method: daily scan after release. Any orphan adds a server-side block as an API ask.

## Telemetry to configure before this ships
| Event | Fired by | Status |
| --- | --- | --- |
| `operation_succeeded {operation: agent_delete}` | client (PostHog) | in 2.1.0 |
| `agent_delete_blocked {numberCount, runCount}` | client (PostHog) | new client event (PostHog) |
| `orphan_deployment (derived, daily scan)` | server | server, API gateway emitter |

**Funnel stages this moves:** none
**Where it is read:** PostHog (client), API gateway emitter (server), cohort read 14 d after the week. KPI owner: Agent config umbrella.

## Research
| Competitor | Status | Note |
| --- | --- | --- |
| Vapi | Not started | Not started: no delete or retire flow captured. |
| Retell | Not started | Not started: no delete or retire flow captured. |
| ElevenLabs | Not started | Not started: no delete or retire flow captured. |
| LiveKit | Not started | Not started: no delete or retire flow captured. |

**Still owed:** Shoot the delete flow at each vendor with a number attached: is it blocked, warned or silent? Question: does any vendor list what uses an agent before deleting it?

## Deliverables (filled by the agent, locked by Shakti)
*   Prototype (opens at the start of Sam's journey): after the run
*   Figma flow, page "v3 · P0 Agent config", section "P0.15 · Retire an agent": after the run
*   Lock: commit on design/v3 once approved
*   Sheet row: https://claude.ai/artifact/6YZCRsJvpoBthj4fnm9XAb
*   Research folder: references/v3/features/P0.15/

## Agent brief (read before designing)
**Scope (sub-tasks):**
- [ ] Delete confirm listing numbers and runs
- [ ] Blocked states with doors to re-point or cancel
- [ ] Type-to-confirm
- [ ] Deleted-agent tags in history (P1.4.g, P2.1.b)
**API:** Partial. DELETE /agents exists. Unconfirmed: whether it is refused while a number or run references the agent. No undo.
**Register rows:** Proposed (new). Not budgeted until the owner signs off.
**Journeys:** IN · Inbound in Studio · BA · Batch run in Studio · CO · Code in Studio · API · API-only, saved agent
**Design days budgeted:** none, proposed
**Rules:** existing Console design system only (docs/design/DESIGN.md on design/v3); reuse, do not redesign; empty first, quiet chrome; sentence case, no arrows or em dashes in prose; locked words from the sheet vocabulary; the persona is Sam and appears only in job text; never change a locked feature unless in scope, then say so in the review post.
**Output of one run:** 1 prototype on design/v3 reachable by URL for the happy path and every rainy state · 2 one screenshot per step of the happy path and per rainy state · 3 the Figma flow: frames in step order, each captioned "Sam does …" with the rationale under it, research shots marked in red · 4 this task moved to in review with a comment (prototype, Figma, commit, rationale) · 5 the sheet row updated · 6 the Slack post "JTBD n of 42 · <id> <title> is done and pending your review", prototype link first, then Figma, then 3 to 5 rationale lines, then open questions.