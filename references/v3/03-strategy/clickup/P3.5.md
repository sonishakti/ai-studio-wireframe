🏷 **V3 · launch Fri 16 Oct 2026** · P3 v3 mapping · design ETA **Thu 8 Oct** · lock ETA **Mon 12 Oct**

## Job to be done (Sam, a developer)
**Job:** Switch between code and hand edits · job map: Modify
**Umbrella job (P3):** Sam wants to look after everything the team's agents share, by hand or from code, and get the same result either way.
**Situation:** Sam sets up an agent from code and adjusts it by hand, or the reverse, without losing a setting.
**Sam wants to:** create agents from a script and tune them in the Console
**So that:** neither side resets the other's work

### P3.5.a Happy path: what Sam does
1. Sam creates an agent with POST /agents from a script.
2. Sam opens it in the builder; every field lands in one home: preset, Advanced, Prompt, Integrations or Deployment.
3. Sam changes the greeting and saves; Studio sends PATCH with only that change.
4. Sam runs GET on the agent; the body matches the script's plus the greeting, nothing else differs.
5. Sam opens the read-only JSON view, which uses API field names, and copies the body back into the script.

### Rainy paths: when it goes wrong
*   **P3.5.b** A field exists only in the API (inbound lifecycle, greeting audio, session actions inject, speak, interrupt): Each gets a home or sits on the approved API-only list with the reason, shown in the JSON view.
*   **P3.5.c** A current Console control has no API field (SAL, voice formatting, recording toggles, provider fallback): Cut, or raised as an API ask; never kept as a control that cannot save.
*   **P3.5.d** Someone saved the agent first (412 PreconditionFailed): Studio shows both versions and keeps Sam's edits to reapply.
*   **P3.5.e** The API rejects a value (InvalidFieldValue, InvalidModuleParameter): The ErrorResponse reason lands on that field in the builder, never a generic toast.
*   **P3.5.f** An agent made by API uses a value no preset holds: The builder opens on Custom and shows the value as is; it never snaps the agent to a preset.
*   **P3.5.g** An agent made by API has no deployment type label: The builder asks Sam once which deployment type it is and stores it in labels.studio_deployment; it never guesses.
*   **P3.5.h** The spec changes under Sam (3.0.0 still moving): The parity script re-runs per spec version; new fields show in the JSON view until they get a home.
*   **P3.5.i** Sam copies the JSON and worries about keys in it: Secrets show as $secrets.<set>.<key> references; values never appear.

## Goal (KPI)
**All 201 API settings have a Console home or a stated reason why not.**
*   Metric: API parity
*   Target: 100 % of 201 spec leaf fields settable in the Console or on the approved API-only list with a reason; renames only Analysis and run, API word in a tooltip
*   How we measure: Parity script over spec ih7axj9zx 3.0.0: fields with a Console home plus fields on the API-only list, share of 201 leaf fields; re-run per spec version
*   Counter-metric: Advanced panel opened by ≤ 30 % of new agents
*   Assumption: The round trip holds for Sam's agents. Method: 20 fixture agents (every pipeline mode, vendor and tool shape) made by API, opened and saved in Studio, return an identical GET body in 20 of 20, per spec version.

## Telemetry
| Event | Fired by | Status |
| --- | --- | --- |
| `operation_failed {errorCode, httpStatus}` | client (PostHog) | in 2.1.0 |
| `advanced_panel_opened` | client (PostHog) | new client |
| `model_slot_configured (port 1.0.0)` | client (PostHog) | new client |
| `agent_updated` | server | server |

Funnel stages this moves: 5 agent_configured (derived) · 6 byok_enabled · 7 app_connected · 9 channel_connected

## Research
| Competitor | Status | Note |
| --- | --- | --- |
| Vapi | Partial | Product. competitors/product/vapi/vapi-09-deprecated-settings.png shows legacy fields kept and named in a Deprecated block; no JSON view or field-name comparison captured. Dashboard signed out since 17 Sep. |
| Retell | Done | Docs and Product. competitors/public-docs/retell-docs-create-agent.png shows the create agent API fields; competitors/product/retell/retell-09-import-dialog.png shows the agents list with Import. No dashboard-to-API field comparison. |
| ElevenLabs | Done | Product. elevenlabs-19-webhook-tool-json.png and elevenlabs-14-test-object-json.png: the Edit as JSON twin exposes defaults the form hides (report-13-22.md §14, §19). Agent-level JSON not shot. |
| LiveKit | Done | Product. livekit-17-trunk-json-editor-validation.png: JSON editor tab with field-path errors; the builder rail flips Live preview and Code (report-13-22.md §17, §19). |

Still owed: Dashboard against API at each vendor: field names, JSON view, API-only fields, round-trip fidelity. Learning: ElevenLabs and LiveKit pair forms with a JSON twin that shows hidden defaults and validates by field path; Vapi names its legacy fields in a Deprecated block instead of hiding them.

## Deliverables
*   Prototype: not yet
*   Figma flow: not yet (page v3 · P3 v3 mapping, section P3.5 · Switch between code and hand edits)
*   Commit: not yet
*   Sheet row: https://claude.ai/artifact/6YZCRsJvpoBthj4fnm9XAb#P3.5

---

###### Footnote for the design agent
Scope: Re-download spec; re-run parity script; Home each missing field by group; Greeting sheet: mode, on, delay, audio URL; Avatar section in Advanced; Cut or raise 25 UI-only items; Map ErrorResponse reasons to fields; 412 sheet; Read-only JSON view using API keys; Map every field to stage 5, 6, 7 or 9; Open API agents on Custom with values as is (new); Ask deployment type once when the label is missing (new).
API: Ready: spec ih7axj9zx 3.0.0 is the source (still moving): 201 leaf fields, 144 audited rows, 25 UI-only items (v3/01-product/api-parity.md). Register: 36, 38 Journeys: B · Build loop · CO · Code in Studio · API · API-only, saved agent · EP · Ephemeral only · FX · Fix loop. Days: 0.75.
Rule 0, data first. This flow shows: production sessions, turns, transcript, recording, error groups and log lines, batch runs and contact lists, secrets and provider keys, integrations (MCP servers, tools), several agents. New account: A new account has one project, no numbers, no secrets and no integrations. Lists open empty; reuse and replace flows need at least two items. Outside our accounts: Vapi and Retell signed-in accounts for numbers and keys pages; ElevenLabs for tools and MCP; the v3 API spec for every field. Create in our account: In our account: 2 secret sets (one referenced by an agent), 3 numbers (one unassigned for 7 days), 2 MCP servers shared by 2 agents, 1 API-created agent to test code and hand edits.
Files: references/v3/features/P3.5/ (jtbd, directions, build spec, flow/ screenshots, shots/ research).
Status: Open = planned · in progress = taken at the next run (08:00, 11:59, 16:00, 20:00) · in review = pending Shakti · completed = locked (Figma frozen, commit) · on hold = parked. Changes: back to in progress + comment `change: …`.
Rules: existing Console design system only (docs/design/DESIGN.md, design/v3); reuse, do not redesign; empty first, quiet chrome; sentence case, no arrows or em dashes; locked words; Sam only in job text; never change a locked feature unless in scope, then say so. One run delivers: prototype for the happy path and every rainy state, one screenshot per step, the Figma flow with "Sam does …" captions and rationale, this task in review with a comment, the sheet row, one Slack post.