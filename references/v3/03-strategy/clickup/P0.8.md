🏷 **V3 · launch Fri 16 Oct 2026** · P0 Agent config · P0.8 · design ETA **Mon 28 Sep** · lock ETA **Wed 30 Sep** · sheet status WIP

**Status is the queue.** Open = planned · in progress = the design agent takes it at the next run (08:00, 11:59, 16:00, 20:00) · in review = delivered, pending Shakti's review · completed = approved and locked (Figma section frozen, commit recorded) · on hold = parked. To ask for changes: move it back to in progress and comment `change: …`.

## Job to be done (Sam, a developer)
**Job:** Confirm it is ready · job map stage **Confirm** (Sam checks it is right before going on)
**Umbrella job (P0):** Sam wants to create a new agent that people outside the team can talk to.
**Situation:** Before people outside the team reach the agent, Sam confirms nothing the agent needs is missing.
**Sam wants to:** go live without surprises
**So that:** the first outside caller meets a finished agent

### P0.8.a Happy path (this is what Sam does)
1. Sam opens Deployment, which comes after Test and is separate from Integrations. The tab for the agent's type opens first.
2. Readiness lists a heard test on this version, the prompt, and a number or contact list, with one fix link each.
3. Sam decides how long what people say to the agent is kept: 30 days or none. This may become its own step; owner to decide.
4. Sam presses Go live inside the same area.

### Rainy paths (each needs its own state in the prototype and a frame in Figma)
*   **P0.8.b** No test was heard since the last change: Readiness names the gap and offers Test. Go live is still allowed.
*   **P0.8.c** Sam has no number or no contact list: go_live_blocked shows its codes, with one fix link each.
*   **P0.8.d** Sam wants zero retention on inbound or batch: Number and Campaign have no data_policy. The option shows disabled, with the reason, until the API adds it.
*   **P0.8.e** The agent already has a deployment: The tab shows the deployment and its status, not an empty form.
*   **P0.8.f** The agent has no deployment type (made through the API): go_live_blocked {code: no_deployment_type} fires, and Studio asks for the type once (P0.1.c).
*   **P0.8.g** The account is suspended at Go live: The minutes banner and Add card show in place. New sessions are refused until reactivation.
*   **P0.8.h** Sam edits an agent that already has a deployment: There is no publish step, so every save reaches callers at once. On save, one line names the number or run using the agent and offers Test (see proposed P0.14).

## Goal (KPI)
**In one line:** 85 in 100 go lives are tested on that exact version before going live.
*   Metric: Test before Go live
*   Target: At least 85 %, as an absolute floor
*   How we measure: channel_connected with a heard test answer (agent_audio_heard, baseline or configured) on the agent version at Go live (agentVersion = updated_at after the last agent_updated), as a share of channel_connected {source console}
*   Counter-metric: TTFDA active median within +10 % of the readiness-off arm
*   Assumption we are testing: A readiness check keeps tests ahead of Go live without slowing it down. Method: flag A/B with readiness on vs off for 2 weeks. Kill if the TTFDA active part grows by more than 10 %.

## Telemetry to configure before this ships
| Event | Fired by | Status |
| --- | --- | --- |
| `readiness_opened (renames preflight_opened)` | client (PostHog) | new client event (PostHog) |
| `go_live_clicked {deploymentType, hasNumber} (renames deploy_clicked)` | client (PostHog) | new client event (PostHog) |
| `go_live_blocked {code: no_deployment_type|no_number|no_list|no_test} (renames deploy_blocked)` | client (PostHog) | new client event (PostHog) |
| `data_policy_selected` | client (PostHog) | new client event (PostHog) |
| `agent_audio_heard (port 1.0.0 + new props surface, agentVersion; agentVersion needs an allowlist key)` | client (PostHog) | new client event (PostHog) |
| `channel_connected` | server | server, API gateway emitter |

**Funnel stages this moves:** 9 channel_connected
**Where it is read:** PostHog (client), API gateway emitter (server), cohort read 14 d after the week. KPI owner: Agent config umbrella.

## Research
| Competitor | Status | Note |
| --- | --- | --- |
| Vapi | Partial | Desk. v3/02-research/batch-retention-experiments.md §2: account-level HIPAA mode. No readiness or retention shot. |
| Retell | Partial | Docs. v3/02-research/monitoring/shots/retell-05-data-storage-settings.png shows per-agent storage tiers: the data policy only. No readiness shot. |
| ElevenLabs | Partial | Product and Desk. competitors/product/elevenlabs/elevenlabs-18-agent-channels.png shows the per-agent Deploy area. Zero Retention Mode, which does not work with batch: desk text in batch-retention-experiments.md §2. No readiness shot. |
| LiveKit | Partial | Product. competitors/product/livekit/livekit-13-observability-settings-pii-redaction.png shows the data setting. livekit-agents.png shows Deploy new agent. No readiness shot. |

**Still owed:** None of the four vendors ships a readiness check before going live. Confirm this with product shots of each vendor's go-live step. Capture the ElevenLabs Zero Retention toggle and the Vapi HIPAA setting in the product. Desk notes: v3/02-research/batch-retention-experiments.md §2.

## Deliverables (filled by the agent, locked by Shakti)
*   Prototype (opens at the start of Sam's journey): after the run
*   Figma flow, page "v3 · P0 Agent config", section "P0.8 · Confirm it is ready": after the run
*   Lock: commit on design/v3 once approved
*   Sheet row: https://claude.ai/artifact/6YZCRsJvpoBthj4fnm9XAb
*   Research folder: references/v3/features/P0.8/

## Agent brief (read before designing)
**Scope (sub-tasks):**
- [ ] Area shell: type tab first, others hidden
- [ ] Readiness rows, one fix link each
- [ ] Data policy: 30 days or none; disabled state
- [ ] Existing-deployment summary state per type
- [ ] Line on save for an agent with a deployment
- [ ] Replace Deploy and Connect copy with Deployment, Go live
**API:** Partial. data_policy exists only on SessionCreate (code). Missing on Number.inbound and Campaign.
**Register rows:** 6, 46. Open: retention as its own step (owner).
**Journeys:** IN · Inbound in Studio · BA · Batch run in Studio · CO · Code in Studio · ST · Stalled at Go live · INT · Integration
**Design days budgeted:** 0.125
**Rules:** existing Console design system only (docs/design/DESIGN.md on design/v3); reuse, do not redesign; empty first, quiet chrome; sentence case, no arrows or em dashes in prose; locked words from the sheet vocabulary; the persona is Sam and appears only in job text; never change a locked feature unless in scope, then say so in the review post.
**Output of one run:** 1 prototype on design/v3 reachable by URL for the happy path and every rainy state · 2 one screenshot per step of the happy path and per rainy state · 3 the Figma flow: frames in step order, each captioned "Sam does …" with the rationale under it, research shots marked in red · 4 this task moved to in review with a comment (prototype, Figma, commit, rationale) · 5 the sheet row updated · 6 the Slack post "JTBD n of 42 · <id> <title> is done and pending your review", prototype link first, then Figma, then 3 to 5 rationale lines, then open questions.