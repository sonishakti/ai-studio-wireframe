🏷 **V3 · launch Fri 16 Oct 2026** · P0 Agent config · P0.7 · design ETA **Mon 28 Sep** · lock ETA **Wed 30 Sep** · sheet status WIP

**Status is the queue.** Open = planned · in progress = the design agent takes it at the next run (08:00, 11:59, 16:00, 20:00) · in review = delivered, pending Shakti's review · completed = approved and locked (Figma section frozen, commit recorded) · on hold = parked. To ask for changes: move it back to in progress and comment `change: …`.

## Job to be done (Sam, a developer)
**Job:** Check it answers as intended · job map stage **Confirm** (Sam checks it is right before going on)
**Umbrella job (P0):** Sam wants to create a new agent that people outside the team can talk to.
**Situation:** Sam checks that the agent answers the way Sam intended before anyone else can reach it.
**Sam wants to:** proof the agent works before anyone else hears it
**So that:** Sam can go live without guessing

### P0.7.a Happy path (this is what Sam does)
1. Sam presses Save and test, and the test panel opens on Talk.
2. Studio saves, starts a test session from agent_id, stores its id and tags client_reference studio_test:.
3. Sam hears the first answer. The transcript runs beside it, with tool calls marked.
4. Sam ends the test and edits again. The Simulations section stays as it is.

### Rainy paths (each needs its own state in the prototype and a frame in Figma)
*   **P0.7.b** Sam has unsaved edits: Sessions start from agent_id, so the test saves first and never tests an old version.
*   **P0.7.c** The browser denies the mic: The panel shows steps to allow it and offers a text simulation, which never counts as an answer.
*   **P0.7.d** The account is suspended or free minutes are used up: test_refused fires, and Add card shows in place.
*   **P0.7.e** Too many sessions are running: test_refused {reason: concurrency_limit} names the limit. Retry works once a session ends.
*   **P0.7.f** The session fails to start (422, 5xx): The panel shows the error code and a retry. If a BYOK key failed, it names the module.
*   **P0.7.g** A tool or MCP server fails mid-test: The failure is marked on the transcript line.
*   **P0.7.h** The session starts but Sam hears nothing: The panel shows the connection state and a speaker check. Retry keeps the transcript.

## Goal (KPI)
**In one line:** Median time to first answer under 3 minutes, and under 6 minutes for 3 in 4.
*   Metric: TTFA, time to first answer
*   Target: Median 3 min or less, p75 6 min or less of active time (source console)
*   How we measure: First builder_opened for the agentId to agent_tested_configured, summing activeMs across builder_opened and builder_resumed sessions, idle over 120 s removed, winsorised at 900 s, source console
*   Counter-metric: VTR at least 70 % (agent_created to agent_tested_configured within 14 d)
*   Assumption we are testing: Aha 1 helps. Agents that reach stage 4 reach stage 8 within 14 d at 1.2× or more the rate of those that skip it. Method: flag A/B that auto-opens the test panel after create. Kill below 1.05×.

## Telemetry to configure before this ships
| Event | Fired by | Status |
| --- | --- | --- |
| `test_panel_opened (port 1.0.0)` | client (PostHog) | new client event (PostHog) |
| `builder_resumed (port 1.0.0)` | client (PostHog) | new client event (PostHog) |
| `agent_test_started (port 1.0.0; configured becomes configuredByUser)` | client (PostHog) | new client event (PostHog) |
| `agent_audio_heard {surface, configuredByUser, turnCount, trigger, agentVersion} (port 1.0.0 + new props surface, agentVersion; agentVersion needs an allowlist key)` | client (PostHog) | new client event (PostHog) |
| `agent_test_ended (port 1.0.0)` | client (PostHog) | new client event (PostHog) |
| `test_refused {reason: suspended|resource_limit|concurrency_limit}` | client (PostHog) | new client event (PostHog) |
| `integration_error_seen` | client (PostHog) | new client event (PostHog) |
| `agent_tested_baseline` | client (PostHog) | new client event (PostHog) |
| `agent_tested_configured` | client (PostHog) | new client event (PostHog) |

**Funnel stages this moves:** 4 agent_tested_baseline · 8 agent_tested_configured
**Where it is read:** PostHog (client), API gateway emitter (server), cohort read 14 d after the week. KPI owner: Agent config umbrella.

## Research
| Competitor | Status | Note |
| --- | --- | --- |
| Vapi | Done | Product. competitors/product/vapi/vapi-assistant-model.png shows Talk in the header. competitors/product/vapi/sip-web-call-room-deleted-error.png shows a failed web test. Also research/15-simulations/02-research/vapi/. |
| Retell | Done | Product and Desk. competitors/product/retell/retell-agent-editor.png shows Test Audio, Test LLM and Run Test, with 'call transfer not supported in Webcall'. Simulation desk text: research/15-simulations/02-research/retell/report.md. |
| ElevenLabs | Done | Product. competitors/product/elevenlabs/elevenlabs-agent-agent.png shows the preview pane with voice and chat. competitors/product/elevenlabs/sip-test-call-permission-denied.png shows the mic denied. |
| LiveKit | Done | Product. competitors/product/livekit/livekit-19-agent-builder-conversation.png shows Live preview, Start call and the Audio, Events and Logs tabs. Also livekit-15-agent-console-idle.png. |

**Still owed:** Rainy shots still missing at every vendor: test refused for quota or concurrency, and testing unsaved changes. Retell simulations exist as report text only.

## Deliverables (filled by the agent, locked by Shakti)
*   Prototype (opens at the start of Sam's journey): after the run
*   Figma flow, page "v3 · P0 Agent config", section "P0.7 · Check it answers as intended": after the run
*   Lock: commit on design/v3 once approved
*   Sheet row: https://claude.ai/artifact/6YZCRsJvpoBthj4fnm9XAb
*   Research folder: references/v3/features/P0.7/

## Agent brief (read before designing)
**Scope (sub-tasks):**
- [ ] Save and test button states
- [ ] Panel opens on Talk; port the simulations section unchanged
- [ ] Mic denied, refused (suspended, minutes, concurrency) and start-failure states
- [ ] Nothing-heard state with connection and speaker check
- [ ] Aha 1: first answer on an untouched preset
- [ ] Transcript marks tool calls and tool errors
**API:** Partial. POST /sessions rtc with agent_id ready. client_reference is writable but never returned. Missing: purpose field. The server anchor (notif 112) is unbuilt.
**Register rows:** 40, 52. Scope: before anyone else can reach the agent; reproducing a production surprise is P1.2.
**Journeys:** B · Build loop · INT · Integration · INT-R · Integration saved, not working · IN · Inbound in Studio · BA · Batch run in Studio · CO · Code in Studio · ST · Stalled at Go live · RK · Free minutes run out
**Design days budgeted:** 0.125
**Rules:** existing Console design system only (docs/design/DESIGN.md on design/v3); reuse, do not redesign; empty first, quiet chrome; sentence case, no arrows or em dashes in prose; locked words from the sheet vocabulary; the persona is Sam and appears only in job text; never change a locked feature unless in scope, then say so in the review post.
**Output of one run:** 1 prototype on design/v3 reachable by URL for the happy path and every rainy state · 2 one screenshot per step of the happy path and per rainy state · 3 the Figma flow: frames in step order, each captioned "Sam does …" with the rationale under it, research shots marked in red · 4 this task moved to in review with a comment (prototype, Figma, commit, rationale) · 5 the sheet row updated · 6 the Slack post "JTBD n of 42 · <id> <title> is done and pending your review", prototype link first, then Figma, then 3 to 5 rationale lines, then open questions.