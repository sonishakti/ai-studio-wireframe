🏷 **V3 · launch Fri 16 Oct 2026** · P2 Monitoring · Sessions · P2.8 · design ETA **Mon 5 Oct** · lock ETA **Wed 7 Oct** · sheet status Planned

**Status is the queue.** Open = planned · in progress = the design agent takes it at the next run (08:00, 11:59, 16:00, 20:00) · in review = delivered, pending Shakti's review · completed = approved and locked (Figma section frozen, commit recorded) · on hold = parked. To ask for changes: move it back to in progress and comment `change: …`.

## Job to be done (Sam, a developer)
**Job:** Pinpoint the slow moment · job map stage **Locate** (Sam finds the inputs, the session or the cause)
**Umbrella job (P2):** Sam wants to find out what happened in a session, or in a set of sessions, and why.
**Situation:** Sam finds the moment a session slowed down or broke, and what held it up.
**Story:** Sam felt a slow moment in a session and wants to see where the time went, so Sam knows what to change.

### P2.8.a Happy path (this is what Sam does)
1. First release: the Latency tab shows a table by turn index with legs end-of-turn detection, transcription delay, LLM time to first token, TTS time to first byte and end to end; unmeasured legs read 'Not measured yet'.
2. The timeline strip shows event markers only.
3. After the turn.finished join is verified on 20 sessions: turns sit on the strip, ?turn= expands a turn in place, and its log lines show in the turn window.
4. 'Jump to first error' selects the first failing turn.
5. 'Open the setting' on a leg (from P1.6) opens that module in the builder.

### Rainy paths (each needs its own state in the prototype and a frame in Figma)
*   **P2.8.b** Turn data not available: Latency table by turn index only; says per-turn timing arrives with session analytics.
*   **P2.8.c** Content is not stored: Zero retention: timing only, no utterances.
*   **P2.8.d** Over 200 turns: Virtualised strip with 'Jump to first error'.
*   **P2.8.e** An old link points to a turn that is not there: 'Turn N is not in this session'; the session opens.
*   **P2.8.f** Log lines fall outside any turn: They read 'Before turn 1' or 'After the session ended'.
*   **P2.8.g** A leg was not measured: 'Not measured yet', never dropped and never zero.
*   **P2.8.h** The session ended before the first turn: Says so and points to Status detail in the header.
*   **P2.8.i** The turn join is not verified yet: The turn strip stays hidden and the table leads until the 20-session check passes.

## Goal (KPI)
**In one line:** Median time to find the slowest or first failing turn under 30 seconds.
*   Metric: Turn found
*   Target: Median ≤ 30 s. Details: completion rate reported beside it.
*   How we measure: session_opened to the first turn_selected {isSlowest or isFirstError: true} in that session, median. Completion: share of session_opened on failed or slow sessions that reach it within 2 min. Measurable only after the 20-session join check; until then the 5-owner task is the only read.
*   Counter-metric: ≥ 3 turn_selected within 3 s ≤ 25 %
*   Assumption we are testing: Selecting a turn with its latency legs beats reading the transcript; the average Sam looks for one slow turn, not the whole session. 5-owner task; kill if fewer than 3 of 5 find it.

## Telemetry to configure before this ships
| Event | Fired by | Status |
| --- | --- | --- |
| `turn_selected {turnIndex, trigger, isFirstError, isSlowest}` | client (PostHog) | new client event (PostHog) |
| `session_block_viewed {block: latency}` | client (PostHog) | new client event (PostHog) |
| `link_copied {object: turn}` | client (PostHog) | new client event (PostHog) |

**Funnel stages this moves:** 12 session_reviewed
**Server gaps that block the read:** G13 (see the sheet, Server gaps)
**Where it is read:** PostHog (client), API gateway emitter (server), cohort read 14 d after the week. KPI owner: Monitoring · Sessions umbrella.

## Research
| Competitor | Status | Note |
| --- | --- | --- |
| Datadog | Partial | Desk. Flame graph and span panel described in monitoring-model.json; no trace shot. |
| LiveKit | Done | Docs and Product. Turn-by-turn playback: livekit-01, livekit-05 (monitoring/shots); latency tails by stage: sip-agents-observability-panels.png (competitors/product/livekit). |
| Vapi | Done | Docs and Product. Latency Summary tab: vapi-05 (monitoring/shots); zero-turn call: sip-call-latency-summary-zero-turns.png; per-leg budget: sip-assistant-latency-per-leg.png (competitors/product/vapi). |
| Retell | Done | Product. Detail Logs with per-turn end-to-end latency: sip-call-detail-logs-e2e-latency.png (competitors/product/retell). |
| ElevenLabs | Done | Product. Per-turn LLM latency under each reply: sip-conversation-transcript-llm-latency.png (competitors/product/elevenlabs). |
| Twilio | Done | Docs. Event stream on a shared time axis: twilio-05 (monitoring/shots). |
| LangSmith | Done | Docs. Turns in a thread with matches highlighted: langsmith-02, langsmith-03 (monitoring/shots). |

**Still owed:** Shoot a populated LiveKit turn view with latency legs; confirm which legs each vendor measures per turn.

## Deliverables (filled by the agent, locked by Shakti)
*   Prototype (opens at the start of Sam's journey): after the run
*   Figma flow, page "v3 · P2 Monitoring · Sessions", section "P2.8 · Pinpoint the slow moment": after the run
*   Lock: commit on design/v3 once approved
*   Sheet row: https://claude.ai/artifact/6YZCRsJvpoBthj4fnm9XAb
*   Research folder: references/v3/features/P2.8/

## Agent brief (read before designing)
**Scope (sub-tasks):**
- [ ] Latency table and leg definitions
- [ ] Event-marker timeline
- [ ] Join verification on 20 sessions
- [ ] Post-join turn selection
- [ ] States incl. zero turns and unverified join
**API:** Deferred in v3 (G13). Partial: v2 turn_latency_list by turn_id without timestamps; end-of-turn and first-token legs not measured by v2. Join unverified until checked on 20 sessions.
**Register rows:** 59 (new); 24
**Journeys:** FX · Fix loop · FX-R · Fix with nothing to read · INT-R · Integration saved, not working · CO-R · Code first session fails · IN · Inbound in Studio · CO · Code in Studio
**Design days budgeted:** 0.375
**Rules:** existing Console design system only (docs/design/DESIGN.md on design/v3); reuse, do not redesign; empty first, quiet chrome; sentence case, no arrows or em dashes in prose; locked words from the sheet vocabulary; the persona is Sam and appears only in job text; never change a locked feature unless in scope, then say so in the review post.
**Output of one run:** 1 prototype on design/v3 reachable by URL for the happy path and every rainy state · 2 one screenshot per step of the happy path and per rainy state · 3 the Figma flow: frames in step order, each captioned "Sam does …" with the rationale under it, research shots marked in red · 4 this task moved to in review with a comment (prototype, Figma, commit, rationale) · 5 the sheet row updated · 6 the Slack post "JTBD n of 42 · <id> <title> is done and pending your review", prototype link first, then Figma, then 3 to 5 rationale lines, then open questions.